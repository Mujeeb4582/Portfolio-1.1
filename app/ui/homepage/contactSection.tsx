// STUB: Plan 02 will implement this component.
// This placeholder exists so the RED-phase test scaffold in tests/contact.test.tsx
// can import and compile. All component tests will fail (RED) because this stub
// renders null — no form, no inputs, no contact links.
export default function ContactSection() {
  return null
}
