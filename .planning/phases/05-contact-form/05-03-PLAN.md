---
phase: 05-contact-form
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified: []
autonomous: false
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05

must_haves:
  truths:
    - "Contact section is visible when scrolling to #contact"
    - "Form validation errors appear on empty submit without page reload"
    - "Submit button disables during submission and shows loading state"
    - "Success/error feedback appears after submission attempt"
    - "Four direct contact link icons are visible alongside the form"
    - "Section animates in on scroll reveal"
    - "Layout is responsive: split on desktop, stacked on mobile"
  artifacts:
    - path: "app/ui/homepage/contactSection.tsx"
      provides: "Verified working contact section"
    - path: "app/api/contact/route.ts"
      provides: "Verified API route"
  key_links:
    - from: "Contact form"
      to: "mujeeburahman4582@gmail.com"
      via: "/api/contact -> Resend"
      pattern: "Visual verification only"
---

<objective>
Human visual and functional verification of the Phase 5 contact form feature across all devices and themes.

Purpose: Automated tests confirm component behavior in jsdom but cannot verify visual quality, layout correctness across viewports, animation smoothness, theme compatibility, or actual email delivery. This checkpoint catches issues invisible to tests.

Output: Human approval that the contact form works correctly and looks consistent with the rest of the portfolio. No code changes are expected — if issues are found, executor will fix them and re-present.
</objective>

<execution_context>
@/Users/engr.mujeeburrahman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/engr.mujeeburrahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-contact-form/05-CONTEXT.md
@.planning/phases/05-contact-form/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm clean build before checkpoint</name>
  <files></files>
  <read_first>
    - .env.local (confirm RESEND_API_KEY is present — set to real key for email delivery testing, or leave as placeholder for visual-only testing)
  </read_first>
  <action>
    Run a production build to confirm no build errors:
    ```bash
    bun run build
    ```
    If the build fails, fix errors before proceeding. Then start the dev server:
    ```bash
    bun run dev
    ```

    Note for the developer: To test actual email delivery (CONT-03), set `RESEND_API_KEY` in `.env.local` to your real Resend API key from resend.com/api-keys. The form UI and validation can be verified visually without a real key — the submission will return a 500 error from the API, which tests the error feedback path (CONT-04).
  </action>
  <verify>
    <automated>bun run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bun run build` exits with code 0 (no build errors)
    - No TypeScript errors in terminal output
  </acceptance_criteria>
  <done>Build passes with no errors. Dev server running. Ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — contact form visual and functional review</name>
  <files></files>
  <action>Present the running portfolio at http://localhost:3000 for human review of all CONT-01 through CONT-05 requirements.</action>
  <verify>
    <automated>bun run test --run tests/contact.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <done>Human types "approved" confirming all visual and functional checks passed.</done>
  <what-built>
    Complete Phase 5 contact form feature:
    - ContactSection with name/email/message fields + React Hook Form + Zod validation
    - Field-level error messages on empty submit
    - Loading state on submit button
    - Success/error inline feedback after submission
    - Four direct contact links (email, WhatsApp, LinkedIn, GitHub)
    - Scroll-reveal AnimateIn wrappers
    - Resend API route at /api/contact
    - All 5 CONT requirements implemented and tested
  </what-built>
  <how-to-verify>
    Open http://localhost:3000 in your browser.

    **1. Navigation to contact section**
    - Click "Contact Me" CTA in the hero section — should scroll to the contact section
    - Alternatively scroll to the bottom of the page
    - Verify the section heading "Contact" appears with the UnderLine decoration
    - Verify the section animates in on scroll entry

    **2. Form structure (CONT-01)**
    - Verify three fields are present: Name (text input), Email (email input), Message (textarea)
    - Verify a "Send Message" submit button is present
    - Verify labels are visible above each field

    **3. Form validation (CONT-02)**
    - Click "Send Message" without filling anything in
    - Verify three error messages appear simultaneously:
      - "Name must be at least 2 characters" below the Name field
      - "Please enter a valid email address" below the Email field
      - "Message must be at least 10 characters" below the Message field
    - No page reload or navigation should occur
    - Fill in valid data — error messages should disappear

    **4. Submit state and feedback (CONT-04)**
    - Fill in: Name "Test User", Email "test@example.com", Message "This is a test message from portfolio"
    - Click "Send Message"
    - While submitting: verify the button shows "Sending..." and is disabled (cannot click again)
    - After submission: verify either a green success message or red error message appears below the button
    - If you have a real RESEND_API_KEY set: verify an email arrives at mujeeburahman4582@gmail.com (CONT-03)
    - If using placeholder key: a 500 error is expected — verify the error feedback message appears

    **5. Contact links (CONT-05)**
    - Verify four icons are visible alongside or below the form
    - Click each link and verify it opens the correct destination:
      - Email icon → opens email client with correct address
      - WhatsApp icon → opens WhatsApp or wa.me link
      - LinkedIn icon → opens LinkedIn profile in new tab
      - GitHub icon → opens GitHub profile in new tab

    **6. Responsive layout**
    - Resize browser to mobile width (< 768px)
    - Verify form and contact links stack vertically (no horizontal overflow)
    - Verify all fields and buttons are accessible at mobile size

    **7. Theme compatibility**
    - Toggle between light, dark, and midnight_steel themes using the nav theme toggle
    - Verify form fields, labels, error text, and contact links are readable in all three themes
    - Verify no contrast issues (white-on-white, invisible borders, etc.)

    **8. Accessibility basics**
    - Tab through the form fields using keyboard — verify Tab order is: Name → Email → Message → Submit
    - Verify error messages are associated with their fields (visual check: error text appears directly below its field)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass.
    Or describe any visual or functional issues found — the executor will fix them and re-present.
  </resume-signal>
</task>

</tasks>

<verification>
Human approval required. All automated tests from Plans 01 and 02 must already be GREEN before this checkpoint begins.

```bash
# Pre-checkpoint automated gate
bun run test --run && bunx tsc --noEmit
```
</verification>

<success_criteria>
- Human confirms: form renders correctly with all three fields and submit button
- Human confirms: validation errors appear on empty submit, disappear on valid input
- Human confirms: submit button disables during submission with "Sending..." text
- Human confirms: success or error feedback appears after submission
- Human confirms: four contact links visible and functional
- Human confirms: layout is responsive on mobile
- Human confirms: all three themes (light/dark/midnight_steel) look correct
- Human types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/05-contact-form/05-03-SUMMARY.md` following the summary template at `@/Users/engr.mujeeburrahman/.claude/get-shit-done/templates/summary.md`.
</output>
