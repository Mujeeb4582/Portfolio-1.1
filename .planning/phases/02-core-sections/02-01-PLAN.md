---
phase: 02-core-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - package.json
  - vitest.config.ts
  - tests/setup.ts
  - tests/hero.test.tsx
  - tests/about.test.tsx
  - tests/skills.test.tsx
  - tests/experience.test.tsx
  - public/mujeeb-resume.pdf
autonomous: true
requirements: [HERO-01, HERO-02, HERO-03, HERO-04, HERO-05, ABOUT-01, ABOUT-02, ABOUT-03, SKILL-01, SKILL-02, SKILL-03, EXP-01, EXP-02, EXP-03, EXP-04]

must_haves:
  truths:
    - "Typography tokens text-h2-u, text-para-m, text-number-m, text-code-m, text-h2-m, text-button-u, text-label-u-m are defined in globals.css @theme {}"
    - "react-icons package is present in package.json dependencies"
    - "vitest and @testing-library/react are present in devDependencies"
    - "All four test files exist and contain failing tests covering all 15 requirements"
    - "public/mujeeb-resume.pdf exists (placeholder) so CV download does not 404"
  artifacts:
    - path: "app/globals.css"
      provides: "Typography tokens for all section components"
      contains: "--font-size-h2-u"
    - path: "vitest.config.ts"
      provides: "Test runner configuration with jsdom"
    - path: "tests/setup.ts"
      provides: "Shared jest-dom matchers setup"
    - path: "tests/hero.test.tsx"
      provides: "Failing tests for HERO-01 through HERO-05"
    - path: "tests/about.test.tsx"
      provides: "Failing tests for ABOUT-01 through ABOUT-03"
    - path: "tests/skills.test.tsx"
      provides: "Failing tests for SKILL-01 through SKILL-03"
    - path: "tests/experience.test.tsx"
      provides: "Failing tests for EXP-01 through EXP-04"
  key_links:
    - from: "app/globals.css @theme {}"
      to: "section components"
      via: "Tailwind text-h2-u, text-para-m etc. utility classes"
      pattern: "--font-size-h2-u"
    - from: "tests/setup.ts"
      to: "vitest.config.ts"
      via: "setupFiles reference"
      pattern: "setupFiles"
---

<objective>
Set up all prerequisites that later plans (02-02 through 02-05) depend on before the first line of section code is written. This plan has three responsibilities: (1) add the 7 typography tokens to globals.css that are currently used in components but NOT defined, (2) install react-icons and the vitest test stack then create failing test scaffolds covering all 15 requirements, and (3) place a placeholder PDF at public/mujeeb-resume.pdf so the CV download link does not 404.

Purpose: Plans 02-02 through 02-05 all execute in parallel. They can only do so safely if their test files already exist and their typography tokens are already defined. Without this plan completing first, executors in later plans would step on each other trying to set up the same infrastructure.

Output: Typography tokens in globals.css, react-icons in package.json, vitest config, 4 test files with failing tests, placeholder PDF.
</objective>

<execution_context>
@/Users/engr.mujeeburrahman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/engr.mujeeburrahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-sections/02-UI-SPEC.md
@.planning/phases/02-core-sections/02-RESEARCH.md
@app/globals.css
@app/lib/constant.ts
@app/lib/types.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add typography tokens to globals.css and install react-icons</name>
  <files>app/globals.css, package.json</files>
  <read_first>
    - app/globals.css — read the ENTIRE file first; tokens must be added inside the existing @theme {} block (lines 8-56), NOT in a new block
    - .planning/phases/02-core-sections/02-UI-SPEC.md — Implementation Notes section 1 (exact CSS values and rationale)
    - package.json — confirm react-icons is not already listed
  </read_first>
  <action>
    Step 1 — Add typography tokens to app/globals.css:

    Inside the existing `@theme {}` block (after line 55, before the closing `}`), append these exact declarations:

    ```css
    /* Typography scale — Phase 2 */
    --font-size-h2-u: 2.25rem;       /* 36px — Inter 700, section headings, hero name/title */
    --font-size-h2-m: 1.75rem;       /* 28px — JetBrains Mono 700, stat accent */
    --font-size-number-m: 1.75rem;   /* 28px — JetBrains Mono 700, stat numbers */
    --font-size-para-m: 1rem;        /* 16px — JetBrains Mono 400, body paragraphs */
    --font-size-code-m: 0.875rem;    /* 14px — JetBrains Mono 400, meta text, date ranges */
    --font-size-button-u: 0.875rem;  /* 14px — Inter 700, button labels */
    --font-size-label-u-m: 0.875rem; /* 14px — Inter 400, stat card sub-labels */
    ```

    Do NOT add `--font-size-logo-m` — that is Phase 1 scope already defined elsewhere. Do NOT create a second `@theme {}` block.

    Step 2 — Install react-icons:

    ```bash
    bun add react-icons
    ```

    Verify react-icons appears in package.json `dependencies` (not devDependencies) after install.
  </action>
  <verify>
    <automated>grep -n "font-size-h2-u\|font-size-para-m\|font-size-number-m\|font-size-code-m\|font-size-button-u\|font-size-label-u-m\|font-size-h2-m" /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/app/globals.css</automated>
    <automated>grep "react-icons" /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/package.json</automated>
  </verify>
  <done>All 7 typography token names appear in globals.css. "react-icons" key is present in package.json dependencies. Zero additional @theme {} blocks were added. No inline styles used.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Install test stack and create failing test scaffolds</name>
  <files>package.json, vitest.config.ts, tests/setup.ts, tests/hero.test.tsx, tests/about.test.tsx, tests/skills.test.tsx, tests/experience.test.tsx</files>
  <read_first>
    - package.json — read scripts section; must add "test" script pointing to vitest
    - app/lib/constant.ts — all data values used in test assertions (PERSONAL_INFO.name, EXPERIENCE array, EDUCATION array, SKILLS array)
    - app/lib/types.ts — SkillCategory values for category heading assertions
    - .planning/phases/02-core-sections/02-RESEARCH.md — Validation Architecture section (exact test commands, requirements-to-test mapping)
    - .planning/phases/02-core-sections/02-VALIDATION.md — Per-Task Verification Map (exact behaviors per requirement)
  </read_first>
  <action>
    Step 1 — Install test dependencies:
    ```bash
    bun add -D vitest @testing-library/react @testing-library/jest-dom @vitejs/plugin-react jsdom
    ```

    Step 2 — Add test script to package.json scripts:
    Add `"test": "vitest"` to the scripts object. Do not modify any other scripts.

    Step 3 — Create vitest.config.ts:
    ```typescript
    import { defineConfig } from 'vitest/config'
    import react from '@vitejs/plugin-react'
    import path from 'path'

    export default defineConfig({
      plugins: [react()],
      test: {
        environment: 'jsdom',
        setupFiles: ['./tests/setup.ts'],
        globals: true,
      },
      resolve: {
        alias: {
          '@': path.resolve(__dirname, '.'),
        },
      },
    })
    ```

    Step 4 — Create tests/setup.ts:
    ```typescript
    import '@testing-library/jest-dom'
    ```

    Step 5 — Create tests/hero.test.tsx (RED — these tests import HeroSection which already exists but will fail assertions on the new design):
    ```typescript
    import { render, screen } from '@testing-library/react'
    import HeroSection from '@/app/ui/homepage/heroSection'

    describe('HeroSection', () => {
      it('HERO-01: renders name "Mujeeb ur Rahman" and title "Full-Stack Web Developer"', () => {
        render(<HeroSection />)
        expect(screen.getByText('Mujeeb ur Rahman')).toBeInTheDocument()
        expect(screen.getByText('Full-Stack Web Developer')).toBeInTheDocument()
      })

      it('HERO-02: renders hero photo with src="/profile.jpg"', () => {
        render(<HeroSection />)
        const img = screen.getByAltText('Mujeeb ur Rahman — Full-Stack Web Developer')
        expect(img).toBeInTheDocument()
      })

      it('HERO-03: renders "Contact Me" and "Download CV" CTA buttons', () => {
        render(<HeroSection />)
        expect(screen.getByText('Contact Me')).toBeInTheDocument()
        expect(screen.getByText('Download CV')).toBeInTheDocument()
      })

      it('HERO-04: social links render with correct aria-labels', () => {
        render(<HeroSection />)
        expect(screen.getByLabelText('GitHub profile')).toBeInTheDocument()
        expect(screen.getByLabelText('LinkedIn profile')).toBeInTheDocument()
        expect(screen.getByLabelText('Send email')).toBeInTheDocument()
        expect(screen.getByLabelText('Contact on WhatsApp')).toBeInTheDocument()
      })

      it('HERO-05: Download CV anchor has href="/mujeeb-resume.pdf" and download attribute', () => {
        render(<HeroSection />)
        const link = screen.getByRole('link', { name: /download cv/i })
        expect(link).toHaveAttribute('href', '/mujeeb-resume.pdf')
        expect(link).toHaveAttribute('download')
      })
    })
    ```

    Step 6 — Create tests/about.test.tsx:
    ```typescript
    import { render, screen } from '@testing-library/react'
    import AboutSection from '@/app/ui/homepage/aboutSection'

    describe('AboutSection', () => {
      it('ABOUT-01: renders bio text from PERSONAL_INFO.bio', () => {
        render(<AboutSection />)
        expect(screen.getByText(/Full-Stack Web Developer with 4\+ years/)).toBeInTheDocument()
      })

      it('ABOUT-02: renders stats with values 4+, 6+, 20+ and correct labels', () => {
        render(<AboutSection />)
        expect(screen.getByText('4+')).toBeInTheDocument()
        expect(screen.getByText('6+')).toBeInTheDocument()
        expect(screen.getByText('20+')).toBeInTheDocument()
        expect(screen.getByText('Years Experience')).toBeInTheDocument()
        expect(screen.getByText('Projects Completed')).toBeInTheDocument()
        expect(screen.getByText('Technologies')).toBeInTheDocument()
      })

      it('ABOUT-03: does NOT render a profile photo in about section', () => {
        render(<AboutSection />)
        const images = screen.queryAllByRole('img')
        images.forEach(img => {
          expect(img).not.toHaveAttribute('src', '/profile.jpg')
        })
      })
    })
    ```

    Step 7 — Create tests/skills.test.tsx:
    ```typescript
    import { render, screen } from '@testing-library/react'
    import SkillsSection from '@/app/ui/homepage/skillsSection'

    describe('SkillsSection', () => {
      it('SKILL-01: renders all 5 category headings', () => {
        render(<SkillsSection />)
        expect(screen.getByText('Frontend')).toBeInTheDocument()
        expect(screen.getByText('Backend')).toBeInTheDocument()
        expect(screen.getByText('Mobile')).toBeInTheDocument()
        expect(screen.getByText('LLM/AI')).toBeInTheDocument()
        expect(screen.getByText('Tools')).toBeInTheDocument()
      })

      it('SKILL-02: renders at least one SVG icon element in the skills section', () => {
        render(<SkillsSection />)
        const svgs = document.querySelectorAll('svg')
        expect(svgs.length).toBeGreaterThan(0)
      })

      it('SKILL-03: renders all 35 skill names', () => {
        render(<SkillsSection />)
        const skillNames = [
          'React', 'Next.js', 'TypeScript', 'JavaScript', 'HTML5', 'CSS3', 'TailwindCSS',
          'Redux', 'Bootstrap', 'shadcn/ui', 'Ant Design',
          'Node.js', 'PostgreSQL', 'Ruby on Rails', 'Express.js', 'Supabase', 'Firebase',
          'MySQL', 'MongoDB', 'Python', 'AWS', 'Ruby',
          'React Native',
          'OpenAI API', 'Gemini API', 'LiteLLM', 'Langfuse', 'Prompt Engineering',
          'Git', 'GitHub', 'Docker', 'Cypress', 'TDD', 'Render', 'Netlify',
        ]
        skillNames.forEach(name => {
          expect(screen.getByText(name)).toBeInTheDocument()
        })
      })
    })
    ```

    Step 8 — Create tests/experience.test.tsx:
    ```typescript
    import { render, screen } from '@testing-library/react'
    import ExperienceSection from '@/app/ui/homepage/experienceSection'

    describe('ExperienceSection', () => {
      it('EXP-01: renders 4 experience entries', () => {
        render(<ExperienceSection />)
        expect(screen.getByText('Wonder Crafts')).toBeInTheDocument()
        expect(screen.getByText('Techiosis')).toBeInTheDocument()
        expect(screen.getByText('RGX Labs')).toBeInTheDocument()
        expect(screen.getByText(/Microverse/)).toBeInTheDocument()
      })

      it('EXP-02: each entry renders role, company, and date range', () => {
        render(<ExperienceSection />)
        expect(screen.getByText('Full-Stack Web Developer')).toBeInTheDocument()
        expect(screen.getByText('May 2023 – Present')).toBeInTheDocument()
        expect(screen.getByText('React Native Developer')).toBeInTheDocument()
        expect(screen.getByText('May 2025 – Dec 2025')).toBeInTheDocument()
      })

      it('EXP-03: Wonder Crafts, Techiosis, RGX Labs, Microverse are all present', () => {
        render(<ExperienceSection />)
        expect(screen.getByText('Wonder Crafts')).toBeInTheDocument()
        expect(screen.getByText('Techiosis')).toBeInTheDocument()
        expect(screen.getByText('RGX Labs')).toBeInTheDocument()
        expect(screen.getAllByText(/Microverse/).length).toBeGreaterThan(0)
      })

      it('EXP-04: education entries for Microverse and NUCES-FAST are present', () => {
        render(<ExperienceSection />)
        expect(screen.getByText('NUCES-FAST')).toBeInTheDocument()
        expect(screen.getByText('Remote Full-Stack Web Development Program')).toBeInTheDocument()
      })
    })
    ```

    Step 9 — Create placeholder PDF at public/mujeeb-resume.pdf:
    ```bash
    printf '%%PDF-1.4\n1 0 obj\n<</Type /Catalog /Pages 2 0 R>>\nendobj\n2 0 obj\n<</Type /Pages /Kids [3 0 R] /Count 1>>\nendobj\n3 0 obj\n<</Type /Page /Parent 2 0 R /MediaBox [0 0 612 792]>>\nendobj\nxref\n0 4\n0000000000 65535 f\n0000000009 00000 n\n0000000058 00000 n\n0000000115 00000 n\ntrailer\n<</Size 4 /Root 1 0 R>>\nstartxref\n190\n%%%%EOF' > /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/public/mujeeb-resume.pdf
    ```

    NOTE on test failures: Tests for ExperienceSection will show a module-not-found error (experienceSection.tsx does not exist yet) and hero/about/skills tests will fail assertions because the existing components have the old design. This is EXPECTED — these are the RED phase tests. Do not fix the failures; they will be resolved by plans 02-02 through 02-05.
  </action>
  <verify>
    <automated>ls /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/tests/ && ls /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/vitest.config.ts</automated>
    <automated>grep '"test"' /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/package.json</automated>
    <automated>grep "vitest\|@testing-library" /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/package.json</automated>
    <automated>ls /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/public/mujeeb-resume.pdf</automated>
    <automated>bunx tsc --noEmit --project /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>vitest.config.ts exists. tests/ directory contains setup.ts, hero.test.tsx, about.test.tsx, skills.test.tsx, experience.test.tsx. package.json has "test" script and devDependencies include vitest and @testing-library/react. public/mujeeb-resume.pdf exists (placeholder). TypeScript compiles without new errors introduced by this task.</done>
</task>

</tasks>

<verification>
Run after both tasks complete:
1. `grep -c "font-size-" app/globals.css` — should return 7 or more (7 new tokens)
2. `grep "react-icons" package.json` — confirms install
3. `grep "vitest" package.json` — confirms dev dep
4. `ls tests/` — shows 5 files: setup.ts, hero.test.tsx, about.test.tsx, skills.test.tsx, experience.test.tsx
5. `ls public/mujeeb-resume.pdf` — file exists
6. `bun run build 2>&1 | tail -5` — build should still succeed (tokens added do not break anything)
</verification>

<success_criteria>
- All 7 typography tokens declared in globals.css @theme {} block with correct rem values
- react-icons in package.json dependencies (not devDependencies)
- vitest, @testing-library/react, @testing-library/jest-dom in devDependencies
- vitest.config.ts exists with jsdom environment and @/ alias
- 5 test files in tests/ directory (setup + 4 section tests)
- Each test file contains describe blocks covering its requirement IDs
- public/mujeeb-resume.pdf exists and is a valid (placeholder) PDF
- `bun run build` still passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-sections/02-01-SUMMARY.md`
</output>
