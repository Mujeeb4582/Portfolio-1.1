---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/globals.css
  - postcss.config.mjs
  - package.json
  - tailwind.config.ts (deleted)
  - app/ui/theme/clientThemeProvider.tsx
  - app/ui/theme/theme-provider.tsx
  - app/ui/theme/theme-toggle.tsx
  - components.json
  - app/ui/button.tsx
  - app/ui/card.tsx
  - app/ui/dropdown-menu.tsx
  - app/ui/avatar.tsx
  - app/ui/input.tsx
  - app/ui/label.tsx
  - app/ui/select.tsx
autonomous: true
requirements:
  - FOUND-03
  - FOUND-04
  - FOUND-06
  - NAV-04
must_haves:
  truths:
    - "Dark mode toggle visually changes the page between light, dark, and midnight_steel themes"
    - "bun run build completes without CSS errors or missing utility warnings"
    - "shadcn/ui components (dropdown, button, card) render with correct animations and styles"
    - "System preference auto-detection works — new tab opening in OS dark mode starts in dark theme"
    - "tailwind.config.ts file is deleted; all tokens live in globals.css @theme {}"
  artifacts:
    - path: "app/globals.css"
      provides: "Tailwind v4 CSS-first config with @theme {}, @custom-variant dark, all three theme blocks"
      contains: "@import \"tailwindcss\""
    - path: "postcss.config.mjs"
      provides: "@tailwindcss/postcss plugin"
      contains: "@tailwindcss/postcss"
    - path: "app/ui/theme/clientThemeProvider.tsx"
      provides: "Updated ThemeProvider with defaultTheme system and 3 themes"
      contains: "defaultTheme=\"system\""
  key_links:
    - from: "app/globals.css"
      to: "dark mode styles"
      via: "@custom-variant dark"
      pattern: "@custom-variant dark"
    - from: "app/globals.css"
      to: "midnight_steel theme"
      via: "@layer base .midnight_steel"
      pattern: "\\.midnight_steel"
    - from: "app/ui/theme/clientThemeProvider.tsx"
      to: "next-themes"
      via: "ThemeProvider attribute=class"
      pattern: "attribute=\"class\""
---

<objective>
Migrate Tailwind CSS from v3 to v4 (CSS-first config), update PostCSS, delete tailwind.config.ts, rewrite globals.css with all design tokens in @theme {}, fix dark mode with @custom-variant, replace tailwindcss-animate with tw-animate-css, re-initialize shadcn/ui for Tailwind v4, and update the theme provider to use system preference detection.

Purpose: This is the core visual infrastructure. Without this plan, dark mode is broken, shadcn animations are broken, and no section work in Phase 2+ can proceed correctly.
Output: Tailwind v4 running, dark mode working, shadcn components re-generated for v4, theme system operational.
</objective>

<execution_context>
@/Users/engr.mujeeburrahman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/engr.mujeeburrahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Key patterns the executor must follow — extracted from RESEARCH.md Code Examples -->

Target globals.css structure:
```css
@import "tailwindcss";
@import "tw-animate-css";

@custom-variant dark (&:where(.dark, .dark *));

@theme {
  /* Brand colors (locked decisions — do NOT change) */
  --color-brand1: #12F7D6;
  --color-brand2: #98FAEC;
  --color-bg1: #292F36;
  --color-bg2: #1A1E23;
  --color-grey: #43454D;
  --color-white: #FFFFFF;

  /* shadcn/ui semantic tokens — light theme defaults */
  --color-background: hsl(0 0% 100%);
  --color-foreground: hsl(20 14.3% 4.1%);
  --color-card: hsl(0 0% 100%);
  --color-card-foreground: hsl(20 14.3% 4.1%);
  --color-popover: hsl(0 0% 100%);
  --color-popover-foreground: hsl(20 14.3% 4.1%);
  --color-primary: hsl(24 9.8% 10%);
  --color-primary-foreground: hsl(60 9.1% 97.8%);
  --color-secondary: hsl(60 4.8% 95.9%);
  --color-secondary-foreground: hsl(24 9.8% 10%);
  --color-muted: hsl(60 4.8% 95.9%);
  --color-muted-foreground: hsl(25 5.3% 44.7%);
  --color-accent: hsl(60 4.8% 95.9%);
  --color-accent-foreground: hsl(24 9.8% 10%);
  --color-destructive: hsl(0 84.2% 60.2%);
  --color-border: hsl(20 5.9% 90%);
  --color-input: hsl(20 5.9% 90%);
  --color-ring: hsl(20 14.3% 4.1%);

  /* Font variables (placeholder until Plan 03 adds Inter/JetBrains) */
  --font-inter: 'Inter', sans-serif;
  --font-jetbrains: 'JetBrains Mono', monospace;

  /* Border radius (FOUND requirement: uncomment and wire --radius) */
  --radius: 0.5rem;
  --radius-lg: var(--radius);
  --radius-md: calc(var(--radius) - 2px);
  --radius-sm: calc(var(--radius) - 4px);

  /* Tech color tokens (used by language badge components) */
  --color-html: #E54F26;
  --color-css: #0C73B9;
  --color-js: #E7A020;
  --color-react-color: #28A9E0;
}

@layer base {
  .dark {
    --color-background: hsl(20 14.3% 4.1%);
    --color-foreground: hsl(60 9.1% 97.8%);
    --color-card: hsl(20 14.3% 4.1%);
    --color-card-foreground: hsl(60 9.1% 97.8%);
    --color-popover: hsl(20 14.3% 4.1%);
    --color-popover-foreground: hsl(60 9.1% 97.8%);
    --color-primary: hsl(60 9.1% 97.8%);
    --color-primary-foreground: hsl(24 9.8% 10%);
    --color-secondary: hsl(12 6.5% 15.1%);
    --color-secondary-foreground: hsl(60 9.1% 97.8%);
    --color-muted: hsl(12 6.5% 15.1%);
    --color-muted-foreground: hsl(24 5.4% 63.9%);
    --color-accent: hsl(12 6.5% 15.1%);
    --color-accent-foreground: hsl(60 9.1% 97.8%);
    --color-destructive: hsl(0 62.8% 30.6%);
    --color-border: hsl(12 6.5% 15.1%);
    --color-input: hsl(12 6.5% 15.1%);
    --color-ring: hsl(24 5.7% 82.9%);
  }

  .midnight_steel {
    --color-background: hsl(210 13% 19%);
    --color-foreground: hsl(210 10% 95%);
    --color-card: hsl(213 15% 12%);
    --color-card-foreground: hsl(213 11% 76%);
    --color-popover: hsl(200 100% 5%);
    --color-popover-foreground: hsl(60 9.1% 97.8%);
    --color-primary: hsl(60 9.1% 97.8%);
    --color-primary-foreground: hsl(200 100% 5%);
    --color-secondary: hsl(200 100% 5%);
    --color-secondary-foreground: hsl(60 9.1% 97.8%);
    --color-muted: hsl(200 100% 5%);
    --color-muted-foreground: hsl(60 9.1% 97.8%);
    --color-accent: hsl(200 100% 5%);
    --color-accent-foreground: hsl(60 9.1% 97.8%);
    --color-destructive: hsl(0 62.8% 30.6%);
    --color-border: hsl(200 100% 5%);
    --color-input: hsl(200 100% 5%);
    --color-ring: hsl(200 100% 5%);
  }

  * {
    @apply border-border;
  }

  body {
    @apply bg-background text-foreground;
  }
}
```

Target postcss.config.mjs:
```javascript
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}
```

Target clientThemeProvider.tsx:
```typescript
'use client'
import { useState, useEffect } from 'react'
import { ThemeProvider } from 'next-themes'

export function ClientThemeProvider({ children }: { children: React.ReactNode }) {
  const [mounted, setMounted] = useState(false)
  useEffect(() => { setMounted(true) }, [])
  if (!mounted) return <>{children}</>
  return (
    <ThemeProvider
      attribute="class"
      defaultTheme="system"
      enableSystem={true}
      themes={['light', 'dark', 'midnight_steel']}
      disableTransitionOnChange
    >
      {children}
    </ThemeProvider>
  )
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install Tailwind v4 packages and run migration codemod</name>
  <files>
    package.json,
    postcss.config.mjs,
    app/globals.css (codemod output — will be revised in Task 2),
    tailwind.config.ts (codemod may delete this)
  </files>
  <read_first>
    - package.json — confirm bun.lock exists and Next.js 16 is installed (prerequisite from Plan 01)
    - postcss.config.mjs — read current plugin config
    - tailwind.config.ts — read before codemod runs (it will be consumed/deleted by codemod)
    - app/globals.css — read current state before codemod transforms it
  </read_first>
  <action>
    Working directory: 

    Step 1 — Install Tailwind v4 packages:
    ```bash
    bun add tailwindcss@^4.2.1 @tailwindcss/postcss@^4.2.1 tw-animate-css@^1.4.0
    bun remove tailwindcss-animate
    ```

    Step 2 — Run the official Tailwind v4 upgrade codemod. This migrates tailwind.config.ts tokens to globals.css @theme {} and renames utilities:
    ```bash
    bunx @tailwindcss/upgrade@next
    ```
    When prompted, select "Yes" to all migration steps.

    Step 3 — Update postcss.config.mjs to use the v4 plugin (codemod may do this automatically — verify and fix if not):
    The file must contain exactly:
    ```javascript
    export default {
      plugins: {
        '@tailwindcss/postcss': {},
      },
    }
    ```

    Step 4 — Verify codemod deleted tailwind.config.ts. If it still exists, delete it:
    ```bash
    rm -f tailwind.config.ts
    ```

    Step 5 — Run build to see what errors the codemod left:
    ```bash
    bun run build 2>&1 | head -40
    ```
    Note all errors — they will be fixed in Task 2.
  </action>
  <verify>
    <automated>
      ls tailwind.config.ts 2>/dev/null || echo "PASS: tailwind.config.ts deleted"
      grep "@tailwindcss/postcss" postcss.config.mjs && echo "PASS: postcss v4 plugin"
      grep "tailwindcss@" package.json && echo "PASS: tailwindcss v4 in package.json"
      grep "tw-animate-css" package.json && echo "PASS: tw-animate-css present"
      grep "tailwindcss-animate" package.json || echo "PASS: tailwindcss-animate removed"
    </automated>
  </verify>
  <done>
    - tailwind.config.ts does not exist
    - postcss.config.mjs uses @tailwindcss/postcss plugin
    - tailwindcss@4.x is in package.json dependencies
    - tailwindcss-animate is not in package.json
    - tw-animate-css is in package.json dependencies
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite globals.css with canonical Tailwind v4 config and fix dark mode</name>
  <files>
    app/globals.css,
    app/ui/theme/clientThemeProvider.tsx,
    app/ui/theme/theme-provider.tsx,
    app/ui/theme/theme-toggle.tsx,
    components.json,
    app/ui/button.tsx,
    app/ui/card.tsx,
    app/ui/dropdown-menu.tsx,
    app/ui/avatar.tsx,
    app/ui/input.tsx,
    app/ui/label.tsx,
    app/ui/select.tsx,
    app/layout.tsx
  </files>
  <read_first>
    - app/globals.css — read the codemod output from Task 1 (base for your rewrite)
    - app/ui/theme/clientThemeProvider.tsx — read current state before updating
    - app/ui/theme/theme-provider.tsx — read current state
    - app/ui/theme/theme-toggle.tsx — read current state (may need ThemeProvider import update)
    - app/layout.tsx — read current state (needs suppressHydrationWarning added)
  </read_first>
  <action>
    Step 1 — Rewrite app/globals.css with the canonical structure shown in the interfaces block above. Critical requirements:
    - First line: `@import "tailwindcss";`
    - Second line: `@import "tw-animate-css";`
    - Third line (blank)
    - Fourth line: `@custom-variant dark (&:where(.dark, .dark *));` — THIS IS THE DARK MODE FIX. Without this exact line, all dark: classes silently produce no CSS.
    - Then the @theme {} block with ALL tokens from the interfaces block
    - Then @layer base with .dark, .midnight_steel, and base * and body rules
    - Remove: the old @tailwind base/components/utilities directives
    - Remove: the old body { font-family: Arial } rule
    - The midnight_steel theme uses ONLY CSS variable overrides (not dark: classes). This is by design.

    Step 2 — Update app/ui/theme/clientThemeProvider.tsx to the exact code shown in the interfaces block. Key change: `defaultTheme="midnight_steel"` → `defaultTheme="system"` plus `enableSystem={true}`. This is the NAV-04 requirement (system preference auto-detection). Also ensure it imports directly from 'next-themes' not from theme-provider wrapper:
    ```typescript
    import { ThemeProvider } from 'next-themes'
    ```

    Step 3 — Update app/ui/theme/theme-provider.tsx. If it's just a thin wrapper re-exporting next-themes ThemeProvider, it can be simplified or the clientThemeProvider can import directly from next-themes (already done in Step 2). Read the file first to decide.

    Step 4 — Update app/layout.tsx: Add `suppressHydrationWarning` to the `<html>` tag. This is REQUIRED when using next-themes — the theme class is injected after initial render and causes React hydration warnings without this attribute:
    ```tsx
    <html lang="en" suppressHydrationWarning>
    ```

    Step 5 — Re-initialize shadcn/ui for Tailwind v4 compatibility. This regenerates all component files (button, card, dropdown-menu, etc.) with v4-compatible class names:
    ```bash
    cd  && bunx shadcn@latest init
    ```
    When prompted:
    - Style: Default
    - Base color: Stone (matches current setup)
    - CSS variables: Yes
    - Overwrite existing files: Yes (regenerates all components for v4)

    Step 6 — After shadcn re-init, manually verify app/ui/button.tsx, app/ui/card.tsx, app/ui/dropdown-menu.tsx do not use:
    - `shadow-sm` (should be `shadow-xs` in v4) — the codemod should handle this but verify
    - `outline-none` (should be `outline-hidden` in v4)
    - `ring` standalone (should be `ring-3` in v4)
    Fix any occurrences found.

    Step 7 — Update components.json if shadcn changed it. Ensure it points to the correct aliases:
    - ui alias: "@/app/ui"
    - utils: "@/app/lib/utils"

    Step 8 — Verify the full build:
    ```bash
    cd  && bun run build
    ```
    Fix any remaining CSS/TypeScript errors.
  </action>
  <verify>
    <automated>
      grep "@custom-variant dark" app/globals.css && echo "PASS: dark variant declared"
      grep "@import \"tailwindcss\"" app/globals.css && echo "PASS: tailwindcss v4 import"
      grep "@import \"tw-animate-css\"" app/globals.css && echo "PASS: tw-animate-css import"
      grep "midnight_steel" app/globals.css && echo "PASS: midnight_steel theme block exists"
      grep "defaultTheme=\"system\"" app/ui/theme/clientThemeProvider.tsx && echo "PASS: system default theme"
      grep "suppressHydrationWarning" app/layout.tsx && echo "PASS: hydration warning suppressed"
      grep "font-family: Arial" app/globals.css || echo "PASS: Arial font-family removed"
      cd  && bun run build 2>&1 | tail -10
    </automated>
  </verify>
  <done>
    - globals.css starts with @import "tailwindcss" and @import "tw-animate-css"
    - @custom-variant dark (&:where(.dark, .dark *)) is present in globals.css
    - @theme {} block contains --color-brand1: #12F7D6 and all three theme variable sets
    - .midnight_steel and .dark blocks are in @layer base
    - clientThemeProvider.tsx has defaultTheme="system" and enableSystem={true}
    - app/layout.tsx html tag has suppressHydrationWarning
    - bun run build exits 0 (no errors)
    - tailwind.config.ts does not exist
    - No "font-family: Arial" in globals.css
  </done>
</task>

</tasks>

<verification>
Run after both tasks complete:

```bash
cd 

# 1. Build must pass
bun run build

# 2. Dark mode critical check
grep "@custom-variant dark" app/globals.css

# 3. All three theme blocks present
grep -E "\.(dark|midnight_steel)" app/globals.css

# 4. No legacy Tailwind v3 directives
grep "@tailwind " app/globals.css || echo "PASS: no v3 directives"

# 5. System preference default
grep "defaultTheme" app/ui/theme/clientThemeProvider.tsx

# 6. TypeScript clean
bunx tsc --noEmit
```
</verification>

<success_criteria>
- `bun run build` exits 0 with no errors
- `bunx tsc --noEmit` exits 0
- `app/globals.css` contains `@custom-variant dark (&:where(.dark, .dark *))` — exact string required
- `app/globals.css` contains `@import "tailwindcss"` and `@import "tw-animate-css"`
- `app/globals.css` contains `.dark` and `.midnight_steel` theme variable blocks
- `tailwind.config.ts` does not exist at project root
- `clientThemeProvider.tsx` has `defaultTheme="system"` and `enableSystem={true}`
- `app/layout.tsx` `<html>` tag has `suppressHydrationWarning`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` documenting:
- Tailwind v4 codemod results (what it changed, what it missed)
- Any shadcn component regeneration issues encountered
- Dark mode verification: confirmed working or issues found
- Confirm `bun run build` and `bun run dev` status
- Any manual class renames applied after codemod (shadow-sm → shadow-xs etc.)
</output>
