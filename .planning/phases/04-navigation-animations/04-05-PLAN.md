---
phase: 04-navigation-animations
plan: 05
type: execute
wave: 4
depends_on: [04-04]
files_modified: []
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03, ANIM-01, ANIM-02, ANIM-03, DEPLOY-02]

must_haves:
  truths:
    - "Clicking a nav link smoothly scrolls to the correct section on desktop"
    - "Navbar turns semi-transparent with backdrop-blur after scrolling"
    - "Active nav link shows cyan underline as user scrolls through sections"
    - "Hamburger appears on mobile, desktop links on larger screens"
    - "Full-screen mobile overlay opens, links are large and readable, menu closes after selection"
    - "Section content fades up as it enters the viewport (scroll-reveal)"
    - "Card grids animate in with visible stagger between cards"
    - "Animations are absent or instant when OS prefers-reduced-motion is enabled"
    - "Layout is correct on mobile (375px), tablet (768px), and desktop (1280px)"
  artifacts: []
  key_links: []
---

<objective>
Human visual and interactive verification of the complete Phase 4 navigation and animation implementation across all devices and motion preferences.

Purpose: Automated tests verify structure and behavior; only a human can confirm the visual quality, smoothness, and feel of animations, the sticky navbar appearance, and responsive layout fidelity.
Output: Confirmed approval or itemized issues for gap closure.
</objective>

<execution_context>
@/Users/engr.mujeeburrahman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/engr.mujeeburrahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-navigation-animations/04-CONTEXT.md
@.planning/phases/04-navigation-animations/04-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 4 implementation:
    - Rebuilt sticky navbar with active section indicator (cyan underline) and backdrop-blur on scroll
    - Mobile hamburger menu with full-screen overlay and animated bars→X transition
    - Smooth scroll to all 5 nav link targets (About, Skills, Projects, Experience, Contact)
    - Scroll-reveal fade-up animations on all section headings and card grids
    - Staggered card entry animations on Skills, Projects, Experience sections
    - Hover effects on project cards (lift + shadow) and skill cards (border glow)
    - prefers-reduced-motion compliance via MotionConfig
    - Contact section placeholder for #contact nav link
  </what-built>
  <how-to-verify>
    Run the dev server: `bun dev`
    Visit: http://localhost:3000

    **Step 1 — Sticky navbar + scroll-aware background (NAV-01)**
    1. Verify navbar is fixed at top with transparent background on page load
    2. Scroll down slightly — navbar should gain semi-transparent background with blur
    3. Scroll back to top — navbar should return to transparent
    Expected: smooth background transition, always visible at top

    **Step 2 — Smooth scroll (NAV-01)**
    1. Click "About" in the navbar — should smooth-scroll to About section
    2. Click "Skills", "Projects", "Experience", "Contact" — each should scroll to its section
    3. Click the "Mujeeb" logo — should scroll back to top (hero)
    Expected: smooth animated scroll, not instant jump

    **Step 3 — Active section indicator (NAV-03)**
    1. Scroll slowly through the page
    2. As each section enters view, its navbar link should highlight with a cyan underline
    3. The underline should transition from one link to the next
    Expected: exactly one link active at a time, cyan (#12F7D6) underline visible

    **Step 4 — Mobile hamburger menu (NAV-02)**
    1. Resize browser to 375px width (or use DevTools mobile emulation — iPhone SE)
    2. Desktop nav links should be hidden; hamburger button should be visible
    3. Click hamburger — three bars should animate to X, full-screen overlay appears
    4. Overlay should show all 5 section links in large readable text (centered)
    5. Click a link — menu closes and scrolls to section
    6. Open menu again — click X button or outside the menu — should close
    7. While menu is open, body should NOT be scrollable (test by trying to scroll)
    Expected: polished hamburger→X animation, full-screen overlay, closes correctly

    **Step 5 — Scroll-reveal animations (ANIM-01)**
    1. Refresh the page
    2. Scroll down slowly through each section
    3. Section headings should fade up into view as they enter the viewport
    4. Skill category cards, project cards, experience entries should stagger in sequentially
    5. Each element should animate once (not re-animate when scrolling back up)
    Expected: subtle fade-up motion, stagger visible on card grids

    **Step 6 — Hover effects (ANIM-02)**
    1. Hover over project cards — should see slight lift (translate-y) and shadow increase
    2. Hover over skill category cards — should see border glow (brand1 color)
    3. Hover over navbar links — should see color transition to cyan
    Expected: smooth CSS transitions, not jarring

    **Step 7 — prefers-reduced-motion (ANIM-03)**
    1. Enable reduced motion: macOS System Settings → Accessibility → Display → Reduce Motion ON
    2. Refresh the page
    3. Scroll through sections — content should appear immediately, no fade-up motion
    4. Nav links and hamburger should still function (no motion just means no animation)
    5. Disable reduced motion after testing
    Expected: all content visible without animation when reduced motion is on

    **Step 8 — Responsive layout (DEPLOY-02)**
    1. Test at 375px (mobile): single column layout, hamburger nav
    2. Test at 768px (tablet): check if layout transitions correctly at md breakpoint
    3. Test at 1280px (desktop): horizontal nav, two-column hero/about layouts
    Expected: no overflow, no broken layouts at any width
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 8 steps pass.
    Or describe specific issues (e.g., "Step 3 — active indicator not updating" or "Step 4 — menu not closing on outside click") to trigger gap closure.
  </resume-signal>
</task>

</tasks>

<verification>
Human approval across all 8 verification steps:
- NAV-01: smooth scroll + sticky background ✓
- NAV-02: hamburger menu ✓
- NAV-03: active section indicator ✓
- ANIM-01: scroll-reveal animations ✓
- ANIM-02: hover effects ✓
- ANIM-03: reduced motion compliance ✓
- DEPLOY-02: responsive layout ✓
</verification>

<success_criteria>
Human approves all 8 verification steps. Phase 4 complete.
</success_criteria>

<output>
After approval, create `.planning/phases/04-navigation-animations/04-05-SUMMARY.md`
</output>
