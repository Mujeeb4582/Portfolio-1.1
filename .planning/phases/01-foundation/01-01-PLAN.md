---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
must_haves:
  truths:
    - "bun run dev starts without errors on Next.js 16 with Turbopack"
    - "bun run build completes without TypeScript or compile errors"
    - "React 19 is the active runtime — no forwardRef deprecation warnings"
    - "pnpm-lock.yaml is gone; bun.lock is present"
  artifacts:
    - path: "package.json"
      provides: "Dependency manifest with Next.js 16 + React 19 + bun scripts"
      contains: "next.*16"
    - path: "bun.lock"
      provides: "Bun lockfile confirming resolved dependency tree"
  key_links:
    - from: "package.json scripts.dev"
      to: "next dev --turbopack"
      via: "bun run dev"
      pattern: "next dev --turbopack"
    - from: "package.json scripts.prepare"
      to: "simple-git-hooks (not husky install)"
      via: "bun run prepare"
      pattern: "simple-git-hooks"
---

<objective>
Migrate the package manager from pnpm to bun, upgrade Next.js 15 → 16 and React 18 → 19, run the official Next.js upgrade codemod to resolve all deprecated async API usages, and configure package.json scripts for the new bun-based toolchain.

Purpose: This is the prerequisite for every other migration step. No Tailwind v4 migration, no shadcn re-init, no ESLint upgrade can happen until the dependency tree is on the correct versions and bun is the active package manager.
Output: Runnable Next.js 16 + React 19 app on bun with clean lockfile.
</objective>

<execution_context>
@/Users/engr.mujeeburrahman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/engr.mujeeburrahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/CONCERNS.md
</context>

<interfaces>
<!-- Current state extracted from codebase — executor reads this before touching files -->

From package.json (current state — to be replaced):
```json
{
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "prepare": "husky install",
    "check-types": "tsc --pretty --noEmit",
    "check-format": "prettier --check .",
    "check-lint": "eslint . --ext ts --ext tsx --ext js",
    "format": "prettier --write ."
  },
  "dependencies": {
    "next": "15.0.3",
    "react": "18",
    "react-dom": "18"
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "eslint": "^8.57.0",
    "eslint-config-next": "15.0.2"
  }
}
```

Target state for package.json scripts after this plan:
```json
{
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "prepare": "simple-git-hooks",
    "check-types": "bunx tsc --noEmit",
    "check-lint": "eslint .",
    "format": "prettier --write ."
  },
  "simple-git-hooks": {
    "pre-commit": "bun run check-lint && bun run check-types"
  }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead font assets and purge pnpm artifacts</name>
  <files>
    app/layout.tsx,
    app/fonts/ (delete GeistVF.woff and GeistMonoVF.woff),
    tailwind.config.ts (remove font-sans/font-mono Geist entries only — do NOT delete the whole file yet),
    node_modules/ (delete),
    pnpm-lock.yaml (delete)
  </files>
  <read_first>
    - app/layout.tsx — read current font imports to know exactly what to remove
    - tailwind.config.ts — read current fontFamily config to know what to strip
    - app/globals.css — verify body font-family:Arial line (will fix in Plan 03)
  </read_first>
  <action>
    Step 1 — Delete Geist font files. These are dead weight and MUST be removed BEFORE the Tailwind codemod runs in Plan 02 (so dead font tokens don't get carried forward into @theme {}):
    ```bash
    rm /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/app/fonts/GeistVF.woff
    rm /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/app/fonts/GeistMonoVF.woff
    ```

    Step 2 — Update app/layout.tsx: Remove the `geistSans` and `geistMono` localFont() declarations and their CSS variable class names from `<body>`. Remove the `import localFont from 'next/font/local'` line entirely.
    The layout.tsx body className should become:
    `className={`${ubuntu.variable} ${ibmPlexMono.variable} antialiased`}`
    (Inter + JetBrains Mono are added in Plan 03 when fonts are fully migrated.)

    Step 3 — Update tailwind.config.ts: Remove only the `sans` and `mono` entries from `fontFamily.extend`:
    ```
    // REMOVE these two lines:
    sans: 'var(--font-geist-sans)',
    mono: 'var(--font-geist-mono)',
    // KEEP: ubuntu and ibmPlexMono entries (they'll be replaced in Plan 03)
    ```
    Do NOT touch any other config — the whole file gets deleted after Tailwind v4 migration in Plan 02.

    Step 4 — Delete pnpm artifacts:
    ```bash
    rm -rf /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/node_modules
    rm /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/pnpm-lock.yaml
    ```
  </action>
  <verify>
    <automated>
      ls /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/app/fonts/ | grep -v "^$" || echo "fonts dir empty or gone"
      # Expect: no GeistVF.woff or GeistMonoVF.woff
      grep -n "geistSans\|geistMono\|localFont\|GeistVF\|GeistMonoVF" /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/app/layout.tsx || echo "PASS: no Geist references"
      grep -n "font-geist-sans\|font-geist-mono" /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/tailwind.config.ts || echo "PASS: no Geist font tokens"
    </automated>
  </verify>
  <done>
    - app/fonts/ contains no Geist .woff files
    - app/layout.tsx has no localFont imports or geistSans/geistMono variables
    - tailwind.config.ts fontFamily.extend has no sans/mono Geist entries
    - node_modules/ and pnpm-lock.yaml are deleted
  </done>
</task>

<task type="auto">
  <name>Task 2: Install Next.js 16 + React 19 with bun and run upgrade codemod</name>
  <files>
    package.json,
    bun.lock (created fresh)
  </files>
  <read_first>
    - package.json — read current dependencies so you know what to preserve and what to update
    - .planning/phases/01-foundation/01-RESEARCH.md — Standard Stack section for exact versions
  </read_first>
  <action>
    Working directory: /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1

    Step 1 — Run the official Next.js upgrade codemod FIRST (before changing package.json). This inserts `await` before any sync async API calls (cookies(), headers(), params, searchParams) and resolves deprecated React 19 patterns:
    ```bash
    bunx @next/codemod@canary upgrade latest
    ```
    If bunx is not available (bun not installed globally yet), install bun first: `npm install -g bun`, then re-run the codemod.

    Step 2 — Rewrite package.json with target dependency versions. Key changes:
    - next: "15.0.3" → "16.1.7"
    - react: "18" → "19.2.4"
    - react-dom: "18" → "19.2.4"
    - @types/react: "^18" → "^19.2.14"
    - @types/react-dom: "^18" → "^19.2.3"
    - @types/node: "^20" → "^22"
    - lucide-react: "^0.462.0" → "^0.577.0"
    - tailwind-merge: "^2.5.5" → "^3.5.0"
    - next-themes: "^0.4.3" → "^0.4.6"
    - eslint-config-next: "15.0.2" → "^16.1.7"
    - REMOVE husky from devDependencies
    - ADD simple-git-hooks: "^2.13.1" to devDependencies
    - KEEP all other existing deps unchanged (tailwindcss, postcss, etc. — those upgrade in Plan 02)

    Update scripts section to:
    ```json
    "scripts": {
      "dev": "next dev --turbopack",
      "build": "next build",
      "start": "next start",
      "prepare": "simple-git-hooks",
      "check-types": "bunx tsc --noEmit",
      "check-lint": "eslint .",
      "format": "prettier --write ."
    }
    ```

    Add simple-git-hooks config at the top level of package.json:
    ```json
    "simple-git-hooks": {
      "pre-commit": "bun run check-lint && bun run check-types"
    }
    ```

    Remove the "lint" script (was `next lint` which is removed in Next.js 16).

    Step 3 — Install with bun:
    ```bash
    bun install
    ```

    Step 4 — Register simple-git-hooks (runs the prepare script to install hooks):
    ```bash
    bun run prepare
    ```

    Step 5 — Remove old Husky directory if it exists:
    ```bash
    rm -rf /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/.husky
    ```

    Step 6 — Verify the build works (Tailwind v3 is still installed at this point — that's fine, it compiles):
    ```bash
    bunx tsc --noEmit
    ```
    Fix any TypeScript errors the codemod surfaced. Common React 19 change: `React.forwardRef` — if any component uses it, update to pass ref as a regular prop. Check app/ui/*.tsx for forwardRef usage.
  </action>
  <verify>
    <automated>
      cd /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1 && node -e "const p=require('./package.json'); console.log(p.dependencies.next, p.dependencies.react)" | grep -E "16\.|19\."
      # Expect: 16.1.7  19.2.4
      ls /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/bun.lock 2>/dev/null && echo "PASS: bun.lock exists"
      ls /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/pnpm-lock.yaml 2>/dev/null || echo "PASS: pnpm-lock.yaml gone"
      ls /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/.husky 2>/dev/null || echo "PASS: .husky dir gone"
      cd /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1 && bunx tsc --noEmit 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    - package.json has next@16.1.7, react@19.2.4, react-dom@19.2.4
    - bun.lock exists at project root
    - pnpm-lock.yaml is gone
    - .husky directory is gone
    - simple-git-hooks is in devDependencies, "prepare" script runs it
    - "simple-git-hooks" config object is in package.json with pre-commit hook
    - bunx tsc --noEmit exits 0 (no TypeScript errors)
  </done>
</task>

</tasks>

<verification>
Run the following after both tasks complete:

```bash
cd /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1

# 1. Verify bun is the active package manager
ls bun.lock && echo "bun.lock present"

# 2. Verify Next.js 16 + React 19
node -e "const p=require('./package.json'); console.log('next:', p.dependencies.next, '| react:', p.dependencies.react)"

# 3. TypeScript clean compile (critical — must be 0 errors)
bunx tsc --noEmit

# 4. Dev server starts (verify no crash on startup — Ctrl+C after seeing "Ready")
timeout 15 bun run dev 2>&1 | head -20
```

All four must pass before this plan is considered complete.
</verification>

<success_criteria>
- `bun run dev` starts Turbopack dev server without errors
- `bunx tsc --noEmit` exits 0
- `package.json` has next@16.1.7, react@19.2.4
- `bun.lock` exists, `pnpm-lock.yaml` does not exist
- No Geist font files in `app/fonts/`
- No Husky artifacts (`.husky/` directory gone, `husky` not in devDependencies)
- `simple-git-hooks` configured in `package.json` with pre-commit hook
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` documenting:
- Exact versions installed (run `bun pm ls` and capture output)
- Any TypeScript errors the codemod fixed
- Any manual React 19 migration needed (forwardRef, deprecated APIs)
- Confirm `bun run dev` status
</output>
