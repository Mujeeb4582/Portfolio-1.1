---
phase: 02-core-sections
plan: 05
type: execute
wave: 3
depends_on: [02-02, 02-03, 02-04]
files_modified:
  - app/ui/homepage/experienceSection.tsx
  - app/page.tsx
autonomous: false
requirements: [EXP-01, EXP-02, EXP-03, EXP-04]

must_haves:
  truths:
    - "Vertical timeline shows 4 work experience entries in order: Wonder Crafts, Techiosis, RGX Labs, Microverse"
    - "Each work entry shows role, company, date range, and bullet responsibilities"
    - "Education entries (Microverse, NUCES-FAST) appear below work experience with a separator"
    - "Work dots are circles (border-brand1), education dots are squares (border-brand2)"
    - "All four sections (Hero, About, Skills, Experience) are rendered in app/page.tsx"
    - "bun run test --run passes (all 15 tests green)"
  artifacts:
    - path: "app/ui/homepage/experienceSection.tsx"
      provides: "Vertical timeline with work experience + education subsection"
      contains: "EXPERIENCE"
    - path: "app/page.tsx"
      provides: "Page composition with all 4 sections"
      contains: "ExperienceSection"
  key_links:
    - from: "experienceSection.tsx"
      to: "EXPERIENCE constant"
      via: "import from @/app/lib/constant"
      pattern: "EXPERIENCE"
    - from: "experienceSection.tsx"
      to: "EDUCATION constant"
      via: "import from @/app/lib/constant"
      pattern: "EDUCATION"
    - from: "app/page.tsx"
      to: "experienceSection.tsx"
      via: "import ExperienceSection"
      pattern: "ExperienceSection"
---

<objective>
Create experienceSection.tsx (new file) as a vertical timeline showing 4 work experience entries and 2 education entries, then wire all four sections into app/page.tsx. This is the final implementation plan for Phase 2.

Purpose: EXP-01 through EXP-04 requirements. After this plan, all 15 Phase 2 requirements are implemented and tests are green.

Output: app/ui/homepage/experienceSection.tsx (new), app/page.tsx updated, tests/experience.test.tsx passing, bun run test --run all green.
</objective>

<execution_context>
@/Users/engr.mujeeburrahman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/engr.mujeeburrahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-sections/02-UI-SPEC.md
@.planning/phases/02-core-sections/02-RESEARCH.md
@app/lib/constant.ts
@app/lib/types.ts
@app/globals.css
@app/ui/card.tsx
@app/ui/underLine.tsx
@app/page.tsx

<interfaces>
<!-- Types and values the experience section consumes -->

From app/lib/types.ts:
```typescript
export interface Experience {
  company: string
  role: string
  dateRange: string
  location: 'Remote'
  responsibilities: string[]
  projects: ProjectReference[]
}

export interface Education {
  institution: string
  degree: string
  location: string
  note?: string
}
```

From app/lib/constant.ts — EXPERIENCE array (4 entries):
- Wonder Crafts | Full-Stack Web Developer | May 2023 – Present | 4 responsibilities
- Techiosis | React Native Developer | May 2025 – Dec 2025 | 3 responsibilities
- RGX Labs | React Native Developer | Nov 2024 – Apr 2025 | 3 responsibilities
- Microverse | Mentor (Volunteer) | Nov 2022 – May 2023 | 3 responsibilities

From app/lib/constant.ts — EDUCATION array (2 entries):
- Microverse | Remote Full-Stack Web Development Program | San Francisco, CA (Remote)
- NUCES-FAST | Bachelor of Engineering, Electrical Engineering | Pakistan | note: "Dean's List"

Timeline dot visual distinction (from UI-SPEC.md):
- Work dot: rounded-full (circle), border-brand1
- Education dot: rounded-sm (square), border-brand2
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create experienceSection.tsx — vertical timeline with work + education</name>
  <files>app/ui/homepage/experienceSection.tsx</files>
  <read_first>
    - app/lib/constant.ts — EXPERIENCE array (exact company names, roles, dateRanges, responsibilities arrays) and EDUCATION array
    - app/lib/types.ts — Experience and Education interfaces
    - app/globals.css — confirm text-h2-u, text-para-m, text-code-m tokens defined
    - app/ui/card.tsx — Card component for content cards
    - app/ui/underLine.tsx — UnderLine for section heading
    - .planning/phases/02-core-sections/02-UI-SPEC.md — Experience Section contract table (connecting line, dot specs, card structure, education separator exact JSX)
    - .planning/phases/02-core-sections/02-RESEARCH.md — Pattern 6 (Pure CSS timeline) and Pattern 7 (Section Heading)
    - tests/experience.test.tsx — all 4 assertions to understand exact text matches
  </read_first>
  <behavior>
    - EXP-01: screen.getByText('Wonder Crafts'), 'Techiosis', 'RGX Labs', and screen.getAllByText(/Microverse/) — all present
    - EXP-02: 'Full-Stack Web Developer', 'May 2023 – Present', 'React Native Developer', 'May 2025 – Dec 2025' — all present
    - EXP-03: same as EXP-01 (company names)
    - EXP-04: screen.getByText('NUCES-FAST') and screen.getByText('Remote Full-Stack Web Development Program') — both present
  </behavior>
  <action>
    Create app/ui/homepage/experienceSection.tsx as a new file:

    ```typescript
    // experienceSection.tsx — new file for Phase 2
    // Vertical timeline: work experience entries + education subsection
    import { EDUCATION, EXPERIENCE } from '@/app/lib/constant'
    import { Card } from '@/app/ui/card'
    import { UnderLine } from '@/app/ui/underLine'

    export default function ExperienceSection() {
      return (
        <section
          id="experience"
          aria-labelledby="experience-heading"
          className="w-full max-w-4xl px-6 py-12"
        >
          {/* Section heading */}
          <div className="mb-12 flex flex-col items-center gap-3 text-center">
            <h2
              id="experience-heading"
              className="font-inter text-h2-u font-bold text-foreground"
            >
              Experience
            </h2>
            <UnderLine />
          </div>

          {/* Timeline */}
          <div className="relative">
            {/* Vertical connecting line */}
            <div
              className="absolute left-4 top-0 h-full w-0.5 bg-brand1/30"
              aria-hidden="true"
            />

            {/* Work experience entries */}
            {EXPERIENCE.map((entry) => (
              <div key={entry.company} className="relative mb-8 pl-12">
                {/* Circle dot for work entries */}
                <div
                  className="absolute left-2.5 top-3 size-3.5 rounded-full border-2 border-brand1 bg-background"
                  aria-hidden="true"
                />

                <Card className="p-6">
                  <h3 className="font-inter text-xl font-bold text-foreground">
                    {entry.role}
                  </h3>
                  <p className="mt-1 font-jetbrains text-code-m text-muted-foreground">
                    {entry.company} &bull; {entry.dateRange}
                  </p>
                  {entry.responsibilities.length > 0 && (
                    <ul className="mt-3 list-disc pl-4 font-jetbrains text-para-m text-foreground">
                      {entry.responsibilities.map((resp) => (
                        <li key={resp} className="mb-1">
                          {resp}
                        </li>
                      ))}
                    </ul>
                  )}
                </Card>
              </div>
            ))}

            {/* Education separator */}
            <div className="relative mb-8 pl-12">
              <div className="flex items-center gap-4">
                <span className="font-inter text-sm font-bold uppercase tracking-widest text-muted-foreground">
                  Education
                </span>
                <div className="h-px flex-1 bg-border" />
              </div>
            </div>

            {/* Education entries */}
            {EDUCATION.map((edu) => (
              <div key={edu.institution} className="relative mb-8 pl-12">
                {/* Square dot for education entries — visually distinct from work */}
                <div
                  className="absolute left-2.5 top-3 size-3.5 rounded-sm border-2 border-brand2 bg-background"
                  aria-hidden="true"
                />

                <Card className="p-6">
                  <h3 className="font-inter text-xl font-bold text-foreground">
                    {edu.institution}
                  </h3>
                  <p className="mt-1 font-jetbrains text-code-m text-muted-foreground">
                    {edu.degree}
                  </p>
                  <p className="font-jetbrains text-code-m text-muted-foreground">
                    {edu.location}
                    {edu.note ? ` • ${edu.note}` : ''}
                  </p>
                </Card>
              </div>
            ))}
          </div>
        </section>
      )
    }
    ```

    Critical constraints:
    - NO `'use client'` directive — Server Component
    - NO hardcoded strings — all content from EXPERIENCE and EDUCATION constants
    - NO inline style={{}} — Tailwind classes only
    - Work dots: `rounded-full border-brand1` (circle)
    - Education dots: `rounded-sm border-brand2` (square, visually distinct)
    - Education entries below separator; separator text is "Education" (exact string, this is NOT a heading — it's a divider label)
    - All responsibilities render as a `<ul>` list
  </action>
  <verify>
    <automated>cd  && bun run test --run tests/experience.test.tsx 2>&1</automated>
    <automated>grep -n "use client\|style={{" app/ui/homepage/experienceSection.tsx && echo "ERROR" || echo "OK"</automated>
    <automated>grep -n "EXPERIENCE\|EDUCATION" app/ui/homepage/experienceSection.tsx</automated>
    <automated>bunx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    - All 4 tests in tests/experience.test.tsx pass (green)
    - experienceSection.tsx has no 'use client' directive
    - EXPERIENCE and EDUCATION are imported from constant (grep confirms)
    - No inline style={{}} in the file
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire all four sections into app/page.tsx and run full test suite</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx — read the CURRENT file; it currently imports Hero, About, Skills; add ExperienceSection import and render
    - app/ui/homepage/experienceSection.tsx — confirm the file was created in Task 1 before adding the import
    - .planning/phases/02-core-sections/02-RESEARCH.md — Architecture Patterns section (section order: Hero → About → Skills → Experience)
  </read_first>
  <action>
    Update app/page.tsx to add ExperienceSection. The current file is:
    ```typescript
    import AboutSection from '@/app/ui/homepage/aboutSection'
    import HeroSection from '@/app/ui/homepage/heroSection'
    import SkillsSection from '@/app/ui/homepage/skillsSection'

    export default function Home() {
      return (
        <main className="flex w-full flex-col items-center justify-center">
          <HeroSection />
          <AboutSection />
          <SkillsSection />
        </main>
      )
    }
    ```

    Add ExperienceSection import at the top and render it after SkillsSection:

    ```typescript
    import AboutSection from '@/app/ui/homepage/aboutSection'
    import ExperienceSection from '@/app/ui/homepage/experienceSection'
    import HeroSection from '@/app/ui/homepage/heroSection'
    import SkillsSection from '@/app/ui/homepage/skillsSection'

    export default function Home() {
      return (
        <main className="flex w-full flex-col items-center justify-center">
          <HeroSection />
          <AboutSection />
          <SkillsSection />
          <ExperienceSection />
        </main>
      )
    }
    ```

    After updating page.tsx, run the full test suite to confirm all 15 tests pass:
    ```bash
    cd  && bun run test --run 2>&1
    ```

    If any tests fail, fix the corresponding section component before considering this task done.

    Finally, run the Next.js build to confirm no build errors:
    ```bash
    cd  && bun run build 2>&1 | tail -20
    ```
  </action>
  <verify>
    <automated>cd  && bun run test --run 2>&1</automated>
    <automated>grep -n "ExperienceSection" app/page.tsx</automated>
    <automated>cd  && bun run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `bun run test --run` outputs 15 tests passed, 0 failed
    - app/page.tsx imports and renders ExperienceSection
    - `bun run build` completes without errors
    - All 4 sections (HeroSection, AboutSection, SkillsSection, ExperienceSection) are in the render tree
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All four sections of Phase 2 are built and wired:
    - Hero: split layout with name, title, tagline, photo, CTAs, social links
    - About: bio text and 3 stat cards (4+, 6+, 20+)
    - Skills: 5 category cards with brand icons for 35 technologies
    - Experience: vertical timeline with 4 work entries + 2 education entries
  </what-built>
  <how-to-verify>
    1. Run `bun run dev` in the project root
    2. Open http://localhost:3000 in a browser
    3. Hero section: confirm name "Mujeeb ur Rahman" is the h1, "Full-Stack Web Developer" is below, bio text is present, photo appears on the right with cyan border, two CTA buttons are visible, 4 social icons are below them
    4. Click "Download CV" — browser should initiate a PDF download (not navigate to a new page)
    5. Scroll to About: confirm "About Me" heading, bio paragraph, 3 stat cards showing 4+, 6+, 20+
    6. Scroll to Skills: confirm 5 category cards (Frontend, Backend, Mobile, LLM/AI, Tools), brand icons visible for known technologies, text-only for unmatched skills
    7. Scroll to Experience: confirm vertical timeline with cyan line, 4 work entries with circle dots, "Education" separator, 2 education entries with square dots
    8. Toggle between light, dark, and midnight_steel themes — all sections should be readable in all three themes
    9. Resize to mobile (< 768px) — hero should stack (text then photo), skills should be single column
  </how-to-verify>
  <resume-signal>Type "approved" to confirm all sections look correct, or describe any visual issues found</resume-signal>
</task>

</tasks>

<verification>
Final phase verification:
1. `bun run test --run` — 15 tests pass, 0 fail
2. `bun run build` — successful build, no TypeScript errors
3. Visual: all sections render at http://localhost:3000 with content from constants
4. Theme toggle: all three themes work on all sections
</verification>

<success_criteria>
- experienceSection.tsx created with vertical timeline (4 work + 2 education entries)
- app/page.tsx renders all 4 sections in order: Hero, About, Skills, Experience
- All 15 tests in tests/ pass: `bun run test --run` shows 15 passed
- `bun run build` completes without errors
- Timeline dots: circles for work (border-brand1), squares for education (border-brand2)
- User visually approves sections in all three themes
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-sections/02-05-SUMMARY.md`
</output>
