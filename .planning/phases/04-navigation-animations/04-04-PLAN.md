---
phase: 04-navigation-animations
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - app/ui/homepage/aboutSection.tsx
  - app/ui/homepage/skillsSection.tsx
  - app/ui/homepage/experienceSection.tsx
  - app/ui/homepage/projectsSection.tsx
  - app/ui/homepage/heroSection.tsx
  - app/page.tsx
autonomous: true
requirements: [ANIM-01, ANIM-02, DEPLOY-02]

must_haves:
  truths:
    - "Section headings animate in on scroll (fade-up via AnimateIn)"
    - "Card grids animate in with stagger delay (StaggerChildren + StaggerItem)"
    - "Hero section has a contact placeholder section with id='contact'"
    - "All existing sections still render their content correctly after wrapping"
    - "Hover effects on project cards and skill cards (transition-all or transition-colors)"
    - "Page layout remains responsive on mobile, tablet, desktop"
  artifacts:
    - path: "app/ui/homepage/aboutSection.tsx"
      provides: "About section with AnimateIn on heading and content"
    - path: "app/ui/homepage/skillsSection.tsx"
      provides: "Skills section with StaggerChildren on skill cards"
    - path: "app/ui/homepage/experienceSection.tsx"
      provides: "Experience section with AnimateIn on heading and StaggerItem on entries"
    - path: "app/ui/homepage/projectsSection.tsx"
      provides: "Projects section with StaggerChildren on project cards"
    - path: "app/page.tsx"
      provides: "Contact placeholder section with id='contact'"
  key_links:
    - from: "app/ui/homepage/*.tsx"
      to: "app/ui/animate-in.tsx"
      via: "AnimateIn wrapping heading divs; StaggerChildren+StaggerItem wrapping card maps"
      pattern: "AnimateIn|StaggerChildren|StaggerItem"
    - from: "app/page.tsx"
      to: "contact placeholder"
      via: "section id='contact' after ProjectsSection"
      pattern: "id=\"contact\""
---

<objective>
Wire AnimateIn and StaggerChildren wrappers into all existing section components, add hover effect classes to interactive cards, and add a contact section placeholder to app/page.tsx so the #contact nav link has a target.

Purpose: This plan adds the visible animation layer to the already-built sections. Each section's Server Component status is preserved — only the thin AnimateIn wrappers are client-side boundaries.
Output: All sections animate on scroll entry. Contact placeholder exists. Full test suite green.
</objective>

<execution_context>
@/Users/engr.mujeeburrahman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/engr.mujeeburrahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-navigation-animations/04-CONTEXT.md
@.planning/phases/04-navigation-animations/04-RESEARCH.md
@.planning/phases/04-navigation-animations/04-03-SUMMARY.md
@app/ui/animate-in.tsx
@app/page.tsx
</context>

<interfaces>
<!-- Animation wrappers from Plan 03 -->

```typescript
// From app/ui/animate-in.tsx:
export function AnimateIn({ children, delay, className }: AnimateInProps): JSX.Element
// Use for: section headings, bio text, individual content blocks

export function StaggerChildren({ children, className }: StaggerProps): JSX.Element
// Use for: card grid containers (map over skills/projects/experience)

export function StaggerItem({ children, className }: StaggerProps): JSX.Element
// Use for: each card inside a StaggerChildren container

// Usage pattern in a Server Component:
import { AnimateIn, StaggerChildren, StaggerItem } from '@/app/ui/animate-in'

// Heading:
<AnimateIn>
  <div className="mb-12 flex flex-col items-center gap-3 text-center">
    <h2>Section Title</h2>
    <UnderLine />
  </div>
</AnimateIn>

// Card grid:
<StaggerChildren className="grid grid-cols-...">
  {items.map((item) => (
    <StaggerItem key={item.id}>
      <Card>...</Card>
    </StaggerItem>
  ))}
</StaggerChildren>
```

Hover effect pattern (ANIM-02 — Claude's discretion):
- Project cards: add `transition-all duration-200 hover:shadow-lg hover:-translate-y-1` to the Card wrapper
- Skill cards: add `transition-colors duration-200 hover:border-brand1/60` to the Card wrapper
- Experience timeline entries: add `transition-colors duration-150 hover:bg-card/80` if cards are used

Section ID audit (all already have correct IDs — confirmed from codebase):
- heroSection.tsx: id="hero" — present
- aboutSection.tsx: id="about" — present
- skillsSection.tsx: id="skills" — present
- projectsSection.tsx: id="projects" — present
- experienceSection.tsx: id="experience" — present
- contact: MISSING — must add placeholder in app/page.tsx
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add AnimateIn wrappers to aboutSection, skillsSection, experienceSection</name>
  <files>
    app/ui/homepage/aboutSection.tsx,
    app/ui/homepage/skillsSection.tsx,
    app/ui/homepage/experienceSection.tsx
  </files>
  <read_first>
    - app/ui/homepage/aboutSection.tsx (full file — wrap heading div + bio paragraph)
    - app/ui/homepage/skillsSection.tsx (full file — wrap heading + skill category cards)
    - app/ui/homepage/experienceSection.tsx (full file — wrap heading + experience entries)
  </read_first>
  <action>
    For each section, import AnimateIn, StaggerChildren, StaggerItem from '@/app/ui/animate-in' and apply wrappers. Do NOT add 'use client' to the section files — AnimateIn is already 'use client' and acts as the client boundary.

    **aboutSection.tsx changes:**
    - Wrap the entire heading block (div with mb-12) in `<AnimateIn>`
    - Wrap the bio paragraph in `<AnimateIn delay={0.1}>`
    - Wrap the stats grid container (div.grid.grid-cols-3) in `<StaggerChildren className="grid grid-cols-3 gap-4 lg:flex-[2]">` and each Card in `<StaggerItem>`
    - Add `transition-colors duration-200 hover:border-brand1/60` to the stats Card className

    **skillsSection.tsx changes:**
    - Read the file to understand the category card structure before wrapping
    - Wrap the section heading block in `<AnimateIn>`
    - For the skills category cards rendering: wrap the outer container in `<StaggerChildren>` and each category card in `<StaggerItem>`
    - Add `transition-colors duration-200 hover:border-brand1/50` to category Card className

    **experienceSection.tsx changes:**
    - Wrap the section heading block in `<AnimateIn>`
    - Wrap the list/container of experience entries in `<StaggerChildren>` and each entry in `<StaggerItem>`
    - For the Education subsection heading: wrap in `<AnimateIn delay={0.1}>`
    - Wrap education entries in `<StaggerChildren>` and each entry in `<StaggerItem>`

    After all three files: run `bunx tsc --noEmit` — 0 errors required.
  </action>
  <verify>
    <automated>bunx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "AnimateIn" app/ui/homepage/aboutSection.tsx — matches found
    - grep -r "StaggerChildren" app/ui/homepage/skillsSection.tsx — matches found
    - grep -r "AnimateIn" app/ui/homepage/experienceSection.tsx — matches found
    - bunx tsc --noEmit exits 0
    - No 'use client' added to these three files
  </acceptance_criteria>
  <done>
    aboutSection, skillsSection, experienceSection have AnimateIn/StaggerChildren wrappers. TypeScript clean. No 'use client' added to these Server Components.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add AnimateIn to projectsSection, heroSection; add contact placeholder to page.tsx</name>
  <files>
    app/ui/homepage/projectsSection.tsx,
    app/ui/homepage/heroSection.tsx,
    app/page.tsx
  </files>
  <read_first>
    - app/ui/homepage/projectsSection.tsx (already 'use client' — can import AnimateIn directly)
    - app/ui/homepage/heroSection.tsx (Server Component — same wrapper pattern as Task 1)
    - app/page.tsx (current — add contact placeholder after ProjectsSection)
  </read_first>
  <action>
    **projectsSection.tsx changes:**
    - This file is already 'use client' — import AnimateIn, StaggerChildren, StaggerItem directly
    - Wrap the section heading block in `<AnimateIn>`
    - Wrap the project cards grid container in `<StaggerChildren className={existing-grid-classes}>` and each project card (FeaturedProjectCard or ProjectCard) in `<StaggerItem>`
    - Add `transition-all duration-200 hover:shadow-lg hover:-translate-y-1` to the Card element inside each project card component (find the outermost Card in BrowserFrame or PhoneFrame card wrappers)
    - Read the file carefully before editing — preserve the existing image error state handling and 'use client' component logic

    **heroSection.tsx changes:**
    - This is a Server Component — do NOT add 'use client'
    - Import AnimateIn from '@/app/ui/animate-in'
    - Wrap the left text content column (div.flex.flex-col.gap-6) in `<AnimateIn>`
    - Wrap the profile image column in `<AnimateIn delay={0.2}>`
    - Social links and CTA buttons inherit animation from the parent wrapper — no additional wrapping needed

    **app/page.tsx changes:**
    - Add a contact placeholder section AFTER `<ProjectsSection />`:
      ```tsx
      {/* Contact placeholder — Phase 5 will replace this with the full ContactSection */}
      <section id="contact" aria-label="Contact" className="w-full py-12" />
      ```
    - This ensures the #contact nav link has a scroll target before Phase 5 builds the form

    After all changes: run `bunx tsc --noEmit` — 0 errors required.
    Run: `bun test` — full suite must pass.
  </action>
  <verify>
    <automated>bun test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep 'id="contact"' app/page.tsx — matches found
    - grep "AnimateIn" app/ui/homepage/heroSection.tsx — matches found
    - grep "StaggerChildren" app/ui/homepage/projectsSection.tsx — matches found
    - bun test exits 0 (full suite green)
    - bunx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    projectsSection and heroSection have AnimateIn wrappers. Contact placeholder section with id="contact" added to page.tsx. Full test suite green. TypeScript clean.
  </done>
</task>

</tasks>

<verification>
- `bun test` — full suite passes (all existing tests + navbar + animate-in + providers)
- `bunx tsc --noEmit` — 0 errors
- grep 'AnimateIn' applied in: aboutSection.tsx, skillsSection.tsx, experienceSection.tsx, projectsSection.tsx, heroSection.tsx
- grep 'StaggerChildren' applied in: aboutSection.tsx, skillsSection.tsx, experienceSection.tsx, projectsSection.tsx
- grep 'id="contact"' in app/page.tsx
- No 'use client' added to aboutSection, skillsSection, experienceSection, heroSection
</verification>

<success_criteria>
All section components have scroll-reveal wrappers. Contact placeholder exists. Hover effects applied. Full test suite green. TypeScript clean.
</success_criteria>

<output>
After completion, create `.planning/phases/04-navigation-animations/04-04-SUMMARY.md`
</output>
