---
phase: 3
slug: projects-section
status: draft
shadcn_initialized: true
preset: default / stone / cssVariables / rsc / tsx
created: 2026-03-19
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for Phase 3: Projects Section.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.
> Inherits from Phase 2 UI-SPEC. Only Phase 3 additions and overrides are declared.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui |
| Preset | style: default, baseColor: stone, cssVariables: true, rsc: true, tsx: true |
| Component library | Radix UI (via shadcn/ui) |
| Icon library | lucide-react (action link icons: Github, ExternalLink) + react-icons/si (already installed) |
| Font — body/heading | Inter (`font-inter`) — CSS var `--font-inter-variable` |
| Font — mono/badges | JetBrains Mono (`font-jetbrains`) — CSS var `--font-jetbrains-variable` |

Source: `components.json` (Phase 1), Phase 2 UI-SPEC (inherited), 03-CONTEXT.md

---

## Spacing Scale

Inherits the Phase 2 8-point scale without change:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gaps, badge padding |
| sm | 8px | Badge internal padding, tech pill gaps |
| md | 16px | Card internal padding, action link gaps |
| lg | 24px | Between card sections (image → content), grid gaps |
| xl | 32px | Between featured card and grid, section inner padding |
| 2xl | 48px | Section vertical padding (top/bottom of Projects section) |
| 3xl | 64px | Reserved — navbar clearance |

Exceptions:
- Featured card screenshot: 400px height on desktop, 240px on mobile — fixed visual weight, not a spacing token
- Grid card screenshot: 200px height on desktop, 160px on mobile — fixed visual weight
- Device frame decoration height: 28px (browser chrome bar) / 32px (phone bezel top) — purely decorative, not part of spacing scale
- Touch targets for action link icon buttons: minimum 44px (use `p-2.5` where needed)

Source: Phase 2 UI-SPEC (inherited); exceptions from 03-CONTEXT.md device frame decision

---

## Typography

Inherits all Phase 2 tokens. No new sizes or weights introduced.

| Token | Font | Size | Weight | Line Height | Usage in Phase 3 |
|-------|------|------|--------|-------------|-----------------|
| `text-h2-u` | Inter | 36px | 700 (bold) | 1.2 | "Projects" section heading |
| `text-para-m` | JetBrains Mono | 16px | 400 (regular) | 1.5 | Project description (2-3 lines), role + company |
| `text-code-m` | JetBrains Mono | 14px | 400 (regular) | 1.5 | Tech stack pill badges |
| `text-button-u` | Inter | 14px | 700 (bold) | 1 | Action link button labels ("Live Demo", "GitHub") |
| `text-label-u-m` | Inter | 14px | 400 (regular) | 1.5 | Project type badge (Web / Mobile), role + company meta |

### Phase 3 Specific Typography Notes

- Project title inside each card: Inter 20px bold (use Tailwind `text-xl font-bold`) — same card-heading tier as experience role titles; no new token needed
- Tech stack pills: JetBrains Mono 14px regular (`text-code-m`) with `px-2 py-0.5` padding and `rounded-full` shape
- Featured card project title: Inter 24px bold (`text-2xl font-bold`) — larger than grid card titles to reinforce featured prominence; no new token, use Tailwind utility directly

Weights used: 400 (regular) and 700 (bold). No intermediate weights.

Source: Phase 2 UI-SPEC (inherited); card title sizing from 03-CONTEXT.md pattern

---

## Color

Inherits Phase 2 color system in full. Phase 3 additions are:

| Role | Light | Dark | Midnight Steel | Usage |
|------|-------|------|---------------|-------|
| Background (dominant 60%) | `hsl(0 0% 100%)` | `hsl(20 14.3% 4.1%)` | `hsl(210 13% 19%)` `#292F36` | Page background, section fill |
| Card surface (secondary 30%) | `hsl(0 0% 100%)` | `hsl(20 14.3% 4.1%)` | `hsl(213 15% 12%)` `#1A1E23` | Project cards (all 6), device frames |
| Brand accent (10%) | `#12F7D6` | `#12F7D6` | `#12F7D6` | See reserved-for list below |
| Browser frame dot 1 | `#FF5F56` (fixed) | same | same | CSS-only browser chrome — red dot |
| Browser frame dot 2 | `#FFBD2E` (fixed) | same | same | CSS-only browser chrome — yellow dot |
| Browser frame dot 3 | `#27C93F` (fixed) | same | same | CSS-only browser chrome — green dot |
| Device frame background | `hsl(var(--muted))` | same | same | Browser URL bar, phone bezel — muted bg |
| Foreground | inherited | inherited | inherited | Card text |
| Muted foreground | inherited | inherited | inherited | Role + company, date meta |
| Screenshot placeholder | `bg-muted` + initials overlay in `text-brand1` | same | same | When screenshotPath image is missing |

### Accent Reserved-For List (Phase 3 Additions)

`#12F7D6` (`brand1`) usage from Phase 2 is unchanged. Phase 3 adds:

10. Project card border on hover: `hover:border-brand1` (transition `border-color duration-200`)
11. Screenshot placeholder initials text: `text-brand1` on `bg-muted` gradient background
12. Featured card top border accent: `border-t-2 border-brand1` — visual prominence marker for Buildable

Source: 03-CONTEXT.md (cyan accent for card hover), Phase 2 UI-SPEC (inherited system)

---

## Component Inventory

All Phase 2 components are available. Phase 3 uses:

| Component | Location | Used In Phase 3 |
|-----------|----------|-----------------|
| `Card`, `CardContent` | `app/ui/card.tsx` | All 6 project cards (featured + grid) |
| `Button` | `app/ui/button.tsx` | Action link buttons ("Live Demo", "GitHub") |
| `UnderLine` | `app/ui/underLine.tsx` | Section heading decoration |
| `next/image <Image>` | next built-in | Project screenshots (lazy-load, not priority) |
| `lucide-react` | installed | `Github` icon (GitHub link), `ExternalLink` icon (live demo link) |
| `react-icons/si` | already installed | NOT used in Phase 3 — tech badges are text-only pills |

Components to AVOID in Phase 3:

| Component | Avoid Because |
|-----------|---------------|
| `Avatar` (`app/ui/avatar.tsx`) | Not needed; use gradient initials placeholder instead |
| `app/ui/skillsCard.tsx` | Pre-Phase-1 artifact; use shadcn `Card` directly |
| `app/ui/languageIcons.tsx` | Not relevant; tech stack shown as text pill badges only |

New component to create:

| Component | Location | Purpose |
|-----------|----------|---------|
| `BrowserFrame` | inline in `projectsSection.tsx` or separate `app/ui/homepage/deviceFrames.tsx` | CSS-only browser chrome decoration for web projects |
| `PhoneFrame` | same file | CSS-only phone bezel decoration for mobile projects |
| `ProjectCard` | inline or `app/ui/homepage/projectCard.tsx` | Single project card (reused for all 6) |
| `FeaturedProjectCard` | inline or same file | Featured (Buildable) full-width variant |
| `projectsSection` | `app/ui/homepage/projectsSection.tsx` | Section root, wired into `app/page.tsx` |

---

## Section-by-Section Interaction Contract

### Projects Section — Overall Layout

```
<section id="projects" aria-labelledby="projects-heading">
  [Section Heading: "Projects" + UnderLine]
  [FeaturedProjectCard — Buildable, full-width]
  [Grid — 2 columns desktop / 1 column mobile — remaining 5 projects]
</section>
```

| Element | Specification |
|---------|--------------|
| Section wrapper | `<section id="projects" aria-labelledby="projects-heading">` py-2xl (48px), max-w-6xl, px-6 |
| Heading | Inter 36px bold, "Projects", centered, + `<UnderLine />` below |
| Heading container | `mb-12 flex flex-col items-center gap-3 text-center` (matches Phase 2 heading pattern) |
| Featured card wrapper | `mb-8` — full width below heading |
| Grid wrapper | `grid grid-cols-1 gap-6 md:grid-cols-2` |
| Grid projects | Projects index 1–5 (MISA App, Uber-like App, Re-View, LSTN, WellShared) |

### Featured Project Card (Buildable — index 0)

| Element | Specification |
|---------|--------------|
| Card root | shadcn `<Card>` with `border border-border hover:border-brand1 transition-colors duration-200 border-t-2 border-t-brand1` |
| Layout | Two-column on desktop: screenshot left (50%), content right (50%) — `md:flex-row` flex |
| Layout mobile | Stacked: screenshot top, content below — default column direction |
| Screenshot area | Left half: `<BrowserFrame>` wrapping `<Image>` — height 400px desktop, 240px mobile |
| Content area | Right half: flex column, p-6, gap-4 between elements |
| Project title | Inter 24px bold (`text-2xl font-bold`), `text-foreground` |
| Role + company | JetBrains Mono 14px regular (`text-code-m`), `text-muted-foreground`, "{role} at {company}" |
| Description | JetBrains Mono 16px regular (`text-para-m`), `text-foreground`, full description (no truncation — featured card has space) |
| Tech stack pills | flex wrap, gap-sm (8px) between pills, `mt-auto` to push to bottom of content column |
| Action links row | flex gap-md (16px), below tech pills |

### Grid Project Cards (indices 1–5)

| Element | Specification |
|---------|--------------|
| Card root | shadcn `<Card>` with `border border-border hover:border-brand1 transition-colors duration-200` |
| Layout | Stacked: screenshot/frame top, content below — always column direction |
| Screenshot area | Top: appropriate frame (`<BrowserFrame>` or `<PhoneFrame>`) wrapping `<Image>` — height 200px desktop, 160px mobile |
| Content area | `p-4`, flex column, gap-3 between elements |
| Project title | Inter 20px bold (`text-xl font-bold`), `text-foreground` |
| Role + company | JetBrains Mono 14px regular (`text-code-m`), `text-muted-foreground`, "{role} at {company}" |
| Description | JetBrains Mono 16px regular (`text-para-m`), `text-foreground`, max 3 lines (`line-clamp-3` Tailwind utility) |
| Tech stack pills | flex wrap, gap-sm (8px), mt-2 |
| Action links row | flex gap-md (16px), mt-auto |

### Browser Frame (Web Projects)

CSS-only decoration above the screenshot image. No image assets.

```
┌─────────────────────────────────┐
│ ● ● ●  [URL bar ────────────]  │  ← 28px height, bg-muted
├─────────────────────────────────┤
│                                 │
│    [screenshot image here]      │
│                                 │
└─────────────────────────────────┘
```

| Element | Specification |
|---------|--------------|
| Frame wrapper | `<div>` with `rounded-t-lg overflow-hidden` |
| Browser bar | `<div className="flex h-7 items-center gap-1.5 bg-muted px-3">` |
| Traffic light dots | 3x `<span className="size-2.5 rounded-full" />` — `bg-[#FF5F56]`, `bg-[#FFBD2E]`, `bg-[#27C93F]` |
| URL bar | `<div className="ml-2 flex-1 rounded bg-background/50 px-2 py-0.5 text-[10px] text-muted-foreground font-mono truncate">` — shows project title or domain placeholder |
| Screenshot container | `<div className="relative overflow-hidden" style={{ height: '{N}px' }}>` |
| Screenshot image | `<Image>` fill layout, `objectFit: "cover"`, `objectPosition: "top"`, `loading="lazy"` |

### Phone Frame (Mobile Projects)

CSS-only decoration around the screenshot image. No image assets.

```
    ╭────────────╮
    │ ■ (notch)  │   ← 32px bezel top, bg-muted
    ├────────────┤
    │            │
    │ [screenshot│
    │  image]    │
    │            │
    ╰────────────╯
```

| Element | Specification |
|---------|--------------|
| Outer wrapper | `<div className="mx-auto max-w-[220px] rounded-[2rem] border-4 border-muted bg-muted p-1 shadow-inner">` |
| Inner screen | `<div className="overflow-hidden rounded-[1.5rem]">` |
| Notch bar | `<div className="flex h-8 items-center justify-center bg-muted">` |
| Notch pill | `<div className="h-3 w-16 rounded-full bg-background/30" aria-hidden="true" />` |
| Screenshot container | `<div className="relative overflow-hidden" style={{ height: '{N}px' }}>` |
| Screenshot image | `<Image>` fill layout, `objectFit: "cover"`, `objectPosition: "top"`, `loading="lazy"` |
| Theme awareness | `bg-muted` resolves correctly across all three themes (light/dark/midnight_steel) |

### Tech Stack Pills

| Element | Specification |
|---------|--------------|
| Pill wrapper | `<span className="font-jetbrains rounded-full bg-muted px-2 py-0.5 text-[0.8125rem] text-muted-foreground">` |
| Font | JetBrains Mono, 13px (0.8125rem) — slightly smaller than `text-code-m` (14px) to visually compress badges |
| No accent color | Pills use `bg-muted text-muted-foreground` — accent is reserved for hover states; pills must not compete with card border accent |
| Overflow handling | `flex flex-wrap gap-2` container; pills wrap naturally — no truncation, no "+N more" pattern |

### Action Links

| Link Type | When to Show | Specification |
|-----------|-------------|--------------|
| "Live Demo" | `liveUrl` exists in Project | `<Button variant="outline" size="sm" asChild><a href={liveUrl} target="_blank" rel="noopener noreferrer"><ExternalLink size={14} /> Live Demo</a></Button>` |
| "GitHub" | `githubUrl` exists in Project | `<Button variant="outline" size="sm" asChild><a href={githubUrl} target="_blank" rel="noopener noreferrer"><Github size={14} /> GitHub</a></Button>` |
| Neither available | both undefined | Render nothing — do not show disabled buttons; omit action links row entirely if no links |

Action link presentation: icon (14px) + text label. NOT icon-only. Claude's Discretion resolved to icon+text for accessibility and clarity (03-CONTEXT.md).

### Screenshot Placeholder (Missing Image Fallback)

Used when `screenshotPath` WebP does not exist in `public/`.

| Element | Specification |
|---------|--------------|
| Container | `<div className="flex h-full w-full items-center justify-center bg-gradient-to-br from-muted to-muted/50">` |
| Initials circle | `<div className="flex size-16 items-center justify-center rounded-full border-2 border-brand1/30 bg-background">` |
| Initials text | First letter of project title, Inter 24px bold, `text-brand1` |
| Alt awareness | This is a visual placeholder only; the surrounding `<Image>` element is replaced, not wrapped |

Implementation: use `onError` on `<Image>` with a state flag to swap to placeholder — this requires `'use client'` on the card only if placeholder is interactive; alternatively render placeholder server-side when screenshotPath is known missing. For Phase 3, treat all 6 as potentially missing and implement client-side `onError` swap pattern on `ProjectCard`.

Note: STATE.md blocker — "6 WebP project screenshots must be created or provided before Phase 3 begins." If screenshots are provided before implementation, the placeholder is a fallback-only path. Do not block Phase 3 implementation waiting for screenshots — build with placeholder, replace on image provision.

---

## Web vs Mobile Visual Distinction

| Dimension | Web Projects (Buildable, Re-View, LSTN, WellShared) | Mobile Projects (MISA App, Uber-like App) |
|-----------|---------------------------------------------------|-----------------------------------------|
| Frame decoration | BrowserFrame (browser chrome bar with traffic lights) | PhoneFrame (rounded bezel with notch) |
| Frame shape | Square/rectangular corners on screenshot | Rounded corners (`rounded-[1.5rem]`) on screenshot |
| Frame width | Full card width | Constrained to `max-w-[220px]` centered — portrait phone ratio |
| Type badge | Optional — determined by `project.type` field in PROJECTS array | Same |
| Determined by | `project.type === 'web'` → render `<BrowserFrame>` | `project.type === 'mobile'` → render `<PhoneFrame>` |

Source: REQUIREMENTS.md PROJ-05, 03-CONTEXT.md (CSS-only frames decision)

---

## Responsive Breakpoints

Inherits Phase 2 breakpoints. Phase 3 additions:

| Breakpoint | Width | Projects Layout |
|------------|-------|----------------|
| Mobile (default) | < 768px | Featured: stacked (screenshot top, content below); Grid: 1 column |
| Tablet (md) | 768px | Featured: 2-column flex (screenshot left, content right); Grid: 2 columns |
| Desktop (lg) | 1024px | Same as tablet — grid remains 2 columns (no 3-column for projects) |
| Max content width | 1152px (max-w-6xl) | Section centered with auto margins |

Note: The grid is intentionally 2-column max (not 3-column like Skills). Project cards need more horizontal space to display descriptions legibly.

Source: 03-CONTEXT.md (2-column grid desktop, 1-column mobile)

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Section heading | "Projects" |
| Section heading — aria-labelledby | id="projects-heading" on the `<h2>` element |
| Live demo button label | "Live Demo" |
| GitHub button label | "GitHub" |
| Screenshot placeholder alt | "{project.title} — screenshot not available" |
| Screenshot image alt | "{project.title} — {project.type === 'web' ? 'web application' : 'mobile application'} screenshot" |
| Role + company meta | "{project.role} at {project.company}" — from PROJECTS array, no hardcoded copy |
| Empty state (no projects) | Not applicable — PROJECTS array always has 6 entries; no empty state |
| Error state | No interactive error states — all content is static Server Component |
| Destructive actions | None in Phase 3 |
| External link `rel` | All external links: `rel="noopener noreferrer"` |
| External link `target` | All external links: `target="_blank"` |
| Screen reader hint for external links | Each `<Button asChild>` wrapping an external `<a>` must include `<span className="sr-only">(opens in new tab)</span>` after the visible label |

Source: REQUIREMENTS.md (PROJ-01 to PROJ-05), 03-CONTEXT.md; defaults applied where upstream was silent

---

## Interaction States

Phase 3 is a static Server Component section. Client-side state is limited to screenshot placeholder swap (onError). Hover states:

| Element | Default State | Hover State |
|---------|--------------|-------------|
| Project card (all 6) | `border-border` | `border-brand1 transition-colors duration-200` |
| Featured card top border | `border-t-brand1` (always visible) | No change — already accent |
| Action link buttons | `border-input bg-background text-foreground` (outline variant) | `bg-accent text-accent-foreground` (shadcn outline default) |
| Screenshot image | default | `scale-105 transition-transform duration-300` (subtle zoom within overflow-hidden container) — Claude's Discretion for hover effect |

Card hover effect decision (Claude's Discretion from 03-CONTEXT.md): border-brand1 transition + image scale-105. This gives meaningful feedback without animations (Phase 4 owns scroll-reveal). Both effects are pure CSS — no JS required.

Note: Phase 4 adds scroll-reveal animations. Phase 3 builds static structure + CSS hover states only.

---

## Implementation Notes for Executor

1. **Server Component by default:** `projectsSection.tsx` is a Server Component (no `'use client'`). The `ProjectCard` sub-component requires `'use client'` ONLY if implementing `onError` screenshot placeholder swap. Prefer to implement the placeholder server-side (known missing paths) to avoid the client boundary.

2. **Image priority:** Hero photo uses `priority` (above fold). Project screenshots use `loading="lazy"` (below fold). Do not set `priority` on any project image.

3. **Heading hierarchy:** Section uses `<h2 id="projects-heading">`. Project titles inside cards use `<h3>`. Do not skip levels.

4. **Featured card identity:** The featured card is always `PROJECTS[0]` (Buildable). No config flag needed — render `PROJECTS[0]` as `<FeaturedProjectCard>` and `PROJECTS.slice(1)` mapped as `<ProjectCard>` in the grid.

5. **Phone frame max-width:** Mobile project screenshots are constrained to `max-w-[220px]` centered within their card to simulate a portrait phone. This means grid card height for mobile projects will differ from web projects — that is intentional and acceptable.

6. **No `screenshotPath` validation:** Do not add runtime path checks. Rely on Next.js Image `onError` for fallback. If screenshots are absent at build time, Next.js will serve 404 for the image src — the `onError` handler triggers the placeholder.

7. **Tech pill font size:** Pills use 13px (not 14px `text-code-m`) to compact badge widths. Use `text-[0.8125rem]` Tailwind arbitrary value — no new token required.

8. **No pagination/filtering:** All 6 projects render at once. No "Load More" button, no filter tabs. (ENH-03 project filtering is v2 scope — out of scope for Phase 3.)

9. **External link safety:** Every `target="_blank"` link must have `rel="noopener noreferrer"`. This is a security requirement from CLAUDE.md (optimized, clean code) and a WCAG 2.2 AA consideration.

10. **wiring in page.tsx:** Import `ProjectsSection` from `app/ui/homepage/projectsSection` and place it after `ExperienceSection` in `app/page.tsx`.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn/ui official | Button, Card (already installed; no new components needed) | not required |
| lucide-react | `Github`, `ExternalLink` icons (already installed) | npm package — standard npm vetting, not a shadcn registry |
| react-icons/si | already installed (Phase 2) — not used in Phase 3 | not applicable |

No third-party shadcn registries declared. Registry vetting gate: not applicable.

Source: 03-CONTEXT.md, Phase 2 UI-SPEC (inherited)

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
