---
phase: 01-foundation
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/ui/homepage/heroSection.tsx
  - app/ui/homepage/aboutSection.tsx
  - app/ui/homepage/skillsSection.tsx
  - app/ui/languageIcons.tsx
  - app/ui/skillsCard.tsx
autonomous: true
requirements:
  - FOUND-07
gap_closure: true

must_haves:
  truths:
    - "No file in the project contains a font-ubuntu or font-ibmPlexMono Tailwind utility class"
    - "All text that previously used font-ubuntu now explicitly uses font-inter"
    - "All text that previously used font-ibmPlexMono now explicitly uses font-jetbrains"
    - "bunx tsc --noEmit exits 0 after the replacements"
    - "bun run check-lint exits 0 after the replacements"
  artifacts:
    - path: "app/ui/homepage/heroSection.tsx"
      provides: "Hero section with all font classes migrated to font-inter / font-jetbrains"
    - path: "app/ui/homepage/aboutSection.tsx"
      provides: "About section with all font classes migrated"
    - path: "app/ui/homepage/skillsSection.tsx"
      provides: "Skills section with all font classes migrated"
    - path: "app/ui/languageIcons.tsx"
      provides: "Language icons component with font class migrated"
    - path: "app/ui/skillsCard.tsx"
      provides: "Skills card component with font class migrated"
  key_links:
    - from: "globals.css @theme {}"
      to: "app/ui/* className attributes"
      via: "font-inter → --font-inter token, font-jetbrains → --font-jetbrains token"
      pattern: "font-inter|font-jetbrains"
---

<objective>
Replace all orphaned `font-ubuntu` and `font-ibmPlexMono` Tailwind utility class names across 5 component files with their correct Phase 1 equivalents: `font-inter` (headings, body) and `font-jetbrains` (code, monospace accents).

Purpose: During the Tailwind v4 migration in Plan 03, layout.tsx and globals.css were updated to Inter + JetBrains Mono, but 5 component files were not updated — they still reference the old font class names from tailwind.config.ts (now deleted). Tailwind v4 silently ignores undefined utilities, so the classes produce no CSS output. FOUND-07 ("dead code, unused imports, placeholder content removed") cannot be fully satisfied until these orphaned class names are eliminated.

Output: 5 component files with all font class references updated. Zero occurrences of font-ubuntu or font-ibmPlexMono in the codebase. All automated checks pass.
</objective>

<execution_context>
@/Users/engr.mujeeburrahman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/engr.mujeeburrahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-VERIFICATION.md
@.planning/phases/01-foundation/01-03-SUMMARY.md

<interfaces>
<!-- Font token definitions established in Plan 03. Executor must use these exact class names. -->
<!-- Source: app/globals.css @theme {} block and app/layout.tsx -->

From app/globals.css @theme {}:
  --font-inter: var(--font-inter-variable), ui-sans-serif, system-ui, sans-serif
  --font-jetbrains: var(--font-jetbrains-variable), ui-monospace, monospace

Tailwind utility classes generated:
  font-inter   → applies --font-inter (Inter — headings, body, UI text)
  font-jetbrains → applies --font-jetbrains (JetBrains Mono — code, tech badges, monospace accents)

Replacement mapping (EXACT):
  font-ubuntu      → font-inter
  font-ibmPlexMono → font-jetbrains

All 22 occurrences to replace (confirmed by grep):
  heroSection.tsx  line  23: font-ibmPlexMono → font-jetbrains
  heroSection.tsx  line  56: font-ubuntu      → font-inter
  heroSection.tsx  line  68: font-ibmPlexMono → font-jetbrains
  heroSection.tsx  line  71: font-ubuntu      → font-inter
  heroSection.tsx  line  76: font-ibmPlexMono → font-jetbrains
  heroSection.tsx  line  81: font-ibmPlexMono → font-jetbrains
  heroSection.tsx  line  84: font-ibmPlexMono → font-jetbrains
  heroSection.tsx  line  88: font-ibmPlexMono → font-jetbrains
  heroSection.tsx  line  93: font-ibmPlexMono → font-jetbrains
  heroSection.tsx  line 108: font-ibmPlexMono → font-jetbrains
  heroSection.tsx  line 111: font-ibmPlexMono → font-jetbrains
  aboutSection.tsx line  11: font-ubuntu      → font-inter
  aboutSection.tsx line  17: font-ibmPlexMono → font-jetbrains
  aboutSection.tsx line  21: font-ibmPlexMono → font-jetbrains
  aboutSection.tsx line  25: font-ibmPlexMono → font-jetbrains
  aboutSection.tsx line  37: font-ibmPlexMono → font-jetbrains
  skillsSection.tsx line 28: font-ubuntu      → font-inter
  skillsSection.tsx line 33: font-ibmPlexMono → font-jetbrains
  skillsSection.tsx line 40: font-ibmPlexMono → font-jetbrains
  skillsSection.tsx line 43: font-ibmPlexMono → font-jetbrains
  languageIcons.tsx line 20: font-ibmPlexMono → font-jetbrains
  skillsCard.tsx   line 24: font-ibmPlexMono → font-jetbrains
  skillsCard.tsx   line 30: font-ibmPlexMono → font-jetbrains
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace orphaned font classes in all 5 component files</name>
  <files>
    app/ui/homepage/heroSection.tsx,
    app/ui/homepage/aboutSection.tsx,
    app/ui/homepage/skillsSection.tsx,
    app/ui/languageIcons.tsx,
    app/ui/skillsCard.tsx
  </files>

  <read_first>
    MANDATORY — read these files before making any changes:
    1. app/ui/homepage/heroSection.tsx — understand current structure, locate all 11 font-ubuntu/font-ibmPlexMono occurrences
    2. app/ui/homepage/aboutSection.tsx — locate all 5 occurrences
    3. app/ui/homepage/skillsSection.tsx — locate all 4 occurrences
    4. app/ui/languageIcons.tsx — locate the 1 occurrence at line 20
    5. app/ui/skillsCard.tsx — locate the 2 occurrences at lines 24, 30
    6. app/globals.css — confirm font-inter and font-jetbrains tokens are defined in @theme {}
  </read_first>

  <action>
    In each of the 5 files, perform these exact text replacements. Use the Read tool to see the current file content, then use the Edit tool to make targeted replacements. Do NOT rewrite entire files — replace only the font class names.

    Replacement rule:
      - Every occurrence of `font-ubuntu`      → replace with `font-inter`
      - Every occurrence of `font-ibmPlexMono` → replace with `font-jetbrains`

    File-by-file instructions:

    **app/ui/homepage/heroSection.tsx** (11 occurrences):
    - Line 23: `font-ibmPlexMono` in className → `font-jetbrains`
    - Line 56: `font-ubuntu` in className → `font-inter`
    - Line 68: `font-ibmPlexMono` → `font-jetbrains`
    - Line 71: `font-ubuntu` → `font-inter`
    - Lines 76, 81, 84, 88, 93, 108, 111: `font-ibmPlexMono` → `font-jetbrains`

    **app/ui/homepage/aboutSection.tsx** (5 occurrences):
    - Line 11: `font-ubuntu` → `font-inter`
    - Lines 17, 21, 25, 37: `font-ibmPlexMono` → `font-jetbrains`

    **app/ui/homepage/skillsSection.tsx** (4 occurrences):
    - Line 28: `font-ubuntu` → `font-inter`
    - Lines 33, 40, 43: `font-ibmPlexMono` → `font-jetbrains`

    **app/ui/languageIcons.tsx** (1 occurrence):
    - Line 20: `font-ibmPlexMono` → `font-jetbrains`

    **app/ui/skillsCard.tsx** (2 occurrences):
    - Lines 24, 30: `font-ibmPlexMono` → `font-jetbrains`

    After all replacements, verify with grep that no occurrence of font-ubuntu or font-ibmPlexMono remains anywhere in the project:
      grep -r "font-ubuntu\|font-ibmPlexMono" app/

    Then run:
      bunx tsc --noEmit
      bun run check-lint

    Both must exit 0. If either fails, fix the issue before proceeding.

    Semantic intent reference (when deciding font-inter vs font-jetbrains):
    - font-inter: headings (h1, h2, h3), body prose, button text, UI labels — readable text
    - font-jetbrains: code-style spans, tech badges, "code comment" aesthetic spans using text-code-* or text-number-* size tokens, monospace accents in hero/about code-comment visual style

    Note: The existing assignments in the files already make this semantic distinction correctly — the replacements are purely mechanical (ubuntu→inter, ibmPlexMono→jetbrains).
  </action>

  <verify>
    <automated>grep -r "font-ubuntu\|font-ibmPlexMono" /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1/app/ && echo "FAIL: orphaned classes remain" || echo "PASS: no orphaned font classes"</automated>
    <automated>cd /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1 && bunx tsc --noEmit && echo "PASS: tsc clean" || echo "FAIL: tsc errors"</automated>
    <automated>cd /Users/engr.mujeeburrahman/personal-project/Portfolio-1.1 && bun run check-lint && echo "PASS: lint clean" || echo "FAIL: lint errors"</automated>
  </verify>

  <acceptance_criteria>
    1. `grep -r "font-ubuntu\|font-ibmPlexMono" app/` returns zero matches (exit code 1 from grep = no matches = PASS)
    2. `bunx tsc --noEmit` exits 0
    3. `bun run check-lint` exits 0
    4. All 5 files contain `font-inter` and/or `font-jetbrains` where the old font classes appeared
    5. No other content in the files was modified (only the font class name strings changed)
  </acceptance_criteria>

  <done>
    Zero occurrences of font-ubuntu or font-ibmPlexMono anywhere in app/. TypeScript and ESLint both pass. FOUND-07 requirement is fully satisfied: no dead class references remain in the codebase.
  </done>
</task>

</tasks>

<verification>
After task completion, verify FOUND-07 is fully closed:

1. `grep -r "font-ubuntu\|font-ibmPlexMono" app/` → must return nothing (grep exits 1)
2. `bunx tsc --noEmit` → exits 0
3. `bun run check-lint` → exits 0
4. `bun run build` → exits 0

Optional human check (from VERIFICATION.md human_verification section):
- Open `bun run dev` in a browser
- Inspect hero, about, and skills sections in DevTools > Elements > Computed > font-family
- Text using font-jetbrains classes should show JetBrains Mono; text using font-inter should show Inter
- No text should fall through to system serif or generic monospace
</verification>

<success_criteria>
The gap is closed when:
- FOUND-07 truth is fully satisfied: "Dead code, unused imports, and placeholder content are removed"
- Zero orphaned font-ubuntu or font-ibmPlexMono class references exist in any component file
- All automated quality checks (tsc, lint, build) pass without errors
- The 5 affected files each use only defined Tailwind font tokens (font-inter, font-jetbrains)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md` following the summary template.

The summary must record:
- requirements-completed: [FOUND-07]
- files modified: all 5 component files
- key-decisions: chosen Option 2 (replace class names) over Option 1 (define dummy tokens) — aligns with Phase 1 intent to fully migrate to Inter + JetBrains Mono
- patterns-established: mechanical font class migration pattern for Tailwind v4 upgrades
</output>
